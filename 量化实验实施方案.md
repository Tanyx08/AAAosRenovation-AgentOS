# AgentOS-AAA 量化实验实施方案

本文档用于规划 AgentOS-AAA 决赛阶段的量化实验。功能测试已经说明“功能是否可用”，量化实验进一步回答：

- AgentOS 内核机制相比普通 xv6 用户态方案带来了哪些收益；
- 文件数量、Agent 数量、消息数量扩大后，系统表现是否仍然稳定；
- 答辩中可以用哪些可复现数据支撑 AgentFS、Context、Agent Loop、调度和多 Agent 协作的价值。

本阶段暂不实现“文件租约与故障恢复”的完整量化图表，只保留已有 `agentlease` 功能测试作为正确性证据。

## 1. 总体目标

量化实验围绕 6 组数据展开：

| 编号 | 实验 | 主要问题 |
|---:|---|---|
| 1 | AgentFS scan/index/cache | 属性索引和 shared cache 是否减少扫描与查询时间 |
| 2 | Context 与 shared cache 复用 | 重复任务是否减少重复查询和工具调用 |
| 3 | plain xv6 与 AgentOS CodeLab 对照 | 同一任务下 AgentOS 是否提供更少扫描、更少轮询和更多系统证据 |
| 4 | `agent_wait` 与轮询 | 事件等待是否减少空转并保持低唤醒延迟 |
| 5 | 调度响应性与公平性 | 高优先级 Agent 是否更快响应，低优先级 Agent 是否不饥饿 |
| 6 | FIFO mailbox 压力 | 并发消息是否不覆盖、不乱序，并能明确统计 dropped/busy |

所有实验统一输出机器可解析的 `[METRIC]` 行，由宿主机脚本转换成 CSV。画图不使用 Python，可以使用电子表格软件、gnuplot、R、Excel/WPS 或手工导入 CSV 的方式完成。

## 2. 输出规范

所有用户态测试程序输出统一格式：

```text
[METRIC] suite=<name> key=value key=value ... status=PASS
```

示例：

```text
[METRIC] suite=agentfs mode=index files=100 run=3 ticks=12 scanned=8 matches=2 status=PASS
[METRIC] suite=context phase=cache_hit cache_hit=1 scanned=0 ticks=1 status=PASS
[METRIC] suite=dual target=plain total_ticks=80 files_scanned=5 polling_loops=30 status=PASS
[METRIC] suite=dual target=agentos total_ticks=45 files_scanned=1 cache_hits=1 context_hits=3 status=PASS
```

宿主机只需要做两件事：

- 从 QEMU 原始日志中过滤 `[METRIC]` 行；
- 将 `key=value` 转成 CSV。

推荐目录结构：

```text
dual-results/
  run_YYYYMMDD_HHMMSS/
    raw/
      agentos_agentfs.log
      agentos_perf.log
      agentos_codelab.log
      baseline_plainlab.log
    csv/
      agentfs.csv
      context.csv
      dual.csv
      wait.csv
      scheduler.csv
      mailbox.csv
    figures/
      放置最终图表图片
```

## 3. baseline-xv6 是否需要修改

需要，但只做最小用户态改动。

baseline-xv6 作为“原生 xv6 对照目标”，不能加入 AgentOS 内核 syscall、Agent Context、AgentFS、动态工具、Agent Loop 或 Agent 调度。它只需要能够运行同一份 CodeLab 工作负载，并输出 plain 指标。

需要添加：

```text
baseline-xv6/repo/main.c
baseline-xv6/repo/todo.c
baseline-xv6/repo/todo.h
baseline-xv6/repo/test.c
baseline-xv6/repo/README
baseline-xv6/user/plainlab.c
```

需要修改 baseline-xv6 的 `Makefile`：

```makefile
UPROGS += \
  $U/_plainlab

UEXTRA += \
  repo/main.c \
  repo/todo.c \
  repo/todo.h \
  repo/test.c \
  repo/README
```

`plainlab.c` 只能使用普通 xv6 能力：

- `open/read/write/close/stat`
- `fork/wait/pipe`
- `sleep/uptime`
- 用户态字符串查找与 patch
- 用户态规则测试

不能包含：

- `kernel/agent.h`
- `agent_create`
- `tool_call`
- `query_file`
- `agent_wait`
- `tool_register`
- 任何 AgentOS syscall

## 4. 实验一：AgentFS scan/index/cache

目标：证明 AgentFS 属性索引和 shared query cache 能减少扫描量和查询时间。

修改位置：

```text
user/agentfsmetric.c
```

建议先使用较小规模，避免 xv6/QEMU 初始化过慢：

```text
files = 10 / 30 / 60 / 100
mode = scan / index / cache
repeat = 30
```

当前代码中新增独立量化程序 `agentfsmetric`，不改动原有功能测试 `agentfsbench`：

```text
agentfsmetric        # 快速验证：10，每组 2 轮
agentfsmetric small  # 小规模采数：10 / 30，每组 2 轮
agentfsmetric full   # 正式采数：10 / 30 / 60 / 100，每组 3 轮
```

如果需要满足论文/答辩图表中的 30 次重复，可以在宿主机脚本中重复运行 `agentfsmetric full` 并合并日志，而不是在单次 xv6 进程里执行过多轮，避免频繁重建文件索引导致 QEMU 等待时间过长。

记录字段：

```text
suite=agentfs
mode=scan|index|cache
files
run
ticks
scanned
matches
cache_hit
status
```

输出示例：

```text
[METRIC] suite=agentfs mode=scan files=60 run=1 ticks=21 scanned=60 matches=3 cache_hit=0 status=PASS
[METRIC] suite=agentfs mode=index files=60 run=1 ticks=6 scanned=8 matches=3 cache_hit=0 status=PASS
[METRIC] suite=agentfs mode=cache files=60 run=1 ticks=1 scanned=0 matches=3 cache_hit=1 status=PASS
```

图表建议：

- 折线图：`files -> P95 ticks`
- 折线图：`files -> scanned`
- 分组柱状图：scan/index/cache 的 median ticks

答辩结论：

```text
文件数量增加时，全量 scan 的 scanned_count 线性增长；index 和 cache 路径显著减少扫描量。
```

## 5. 实验二：Context 与 shared cache 复用

目标：展示重复任务中，shared query cache 能减少重复扫描；如果当前 Context 尚未真正跳过工具调用，则只统计 shared cache，不夸大 Context 收益。

修改位置：

```text
user/contextmetric.c
```

当前代码中新增独立量化程序 `contextmetric`，不改动原有功能测试 `agentinnovationtest`：

```text
contextmetric
```

实验阶段：

```text
phase=first        首次 query_file，cache_hit=0
phase=cache_hit    第二个 Agent 重复查询，cache_hit=1
phase=invalidated  修改属性后再次查询，cache_hit=0
```

记录字段：

```text
suite=context
phase
cache_hit
context_hit
tool_calls
query_file_calls
scanned
ticks
saved_ticks
status
```

输出示例：

```text
[METRIC] suite=context phase=first cache_hit=0 context_hit=0 tool_calls=1 scanned=14 ticks=5 status=PASS
[METRIC] suite=context phase=cache_hit cache_hit=1 context_hit=0 tool_calls=1 scanned=0 ticks=1 saved_ticks=4 status=PASS
[METRIC] suite=context phase=invalidated cache_hit=0 context_hit=0 tool_calls=1 scanned=14 ticks=5 status=PASS
```

图表建议：

- 三阶段柱状图：first/cache_hit/invalidated 的 ticks；
- 三阶段柱状图：first/cache_hit/invalidated 的 scanned；
- 如果后续实现真正 Context 复用，再加 `skipped_tool_calls` 图。

答辩结论：

```text
跨 Agent 重复查询不再重新扫描文件系统，文件属性变化后旧缓存会失效，避免返回过期结果。
```

## 6. 实验三：plain xv6 与 AgentOS CodeLab 等价对照

目标：同一份 CodeLab 任务分别在 plain xv6 和 AgentOS-AAA 上完成，先证明最终结果等价，再比较系统机制收益。

### 6.1 baseline plain 路径

新增：

```text
baseline-xv6/user/plainlab.c
```

plain 流程：

```text
1. 扫描 repo 下文件；
2. 读取 repo/todo.c；
3. 定位 delete_task 缺少 task_count--；
4. 用字符串替换修复 todo.c；
5. 读取 repo/test.c；
6. 执行规则测试；
7. 输出 summary 和 metric。
```

plain 输出：

```text
[PLAIN] task=fix_todo_delete status=PASS
[PLAIN] file=repo/todo.c found=1
[PLAIN] patch=task_count-- status=PASS
[PLAIN] test=rule_test status=PASS
[METRIC] suite=dual target=plain total_ticks=80 files_scanned=5 polling_loops=30 duplicate_queries=2 status=PASS
```

### 6.2 AgentOS 路径

使用当前：

```text
planner_agent
```

建议补充机器可解析输出：

```text
[AGENTOS] task=fix_todo_delete status=PASS
[AGENTOS] file=repo/todo.c found=1
[AGENTOS] patch=task_count-- status=PASS
[AGENTOS] test=rule_test status=PASS
[METRIC] suite=dual target=agentos total_ticks=45 files_scanned=1 tool_calls=6 cache_hits=1 context_hits=0 status=PASS
```

记录字段：

```text
suite=dual
target=plain|agentos
total_ticks
files_scanned
tool_calls
syscalls
polling_loops
idle_ticks
duplicate_queries
context_hits
cache_hits
status
```

图表建议：

- 分组柱状图：plain vs agentos 的 total_ticks；
- 分组柱状图：plain vs agentos 的 files_scanned；
- 分组柱状图：plain vs agentos 的 polling_loops；
- 表格：plain 与 agentos 的最终 patch/test/review 是否等价。

答辩结论：

```text
两条路径得到相同业务结果；AgentOS 路径额外提供 AgentFS、Context、事件等待、动态工具、调度和审计证据。
```

## 7. 实验四：agent_wait 与轮询

目标：证明 `agent_wait` 让 Agent 在无事件时睡眠，而不是像普通用户态轮询一样消耗 CPU。

修改位置：

```text
user/waitmetric.c
```

当前代码中新增独立量化程序 `waitmetric`，不改动原有 `agentperftest`：

```text
waitmetric        # 快速验证：delay=10，每组 1 轮
waitmetric small  # 小规模采数：delay=10/20，每组 3 轮
waitmetric full   # 正式采数：delay=10/20/50，每组 30 轮
```

实验变量：

```text
event = message / filemod
delay = 10 / 20 / 50 ticks
repeat = 30
```

记录字段：

```text
suite=wait
event
delay
run
trigger_tick
handle_tick
wakeup_latency
polling_iterations
cpu_ticks
status
```

输出示例：

```text
[METRIC] suite=wait event=message delay=20 run=3 trigger_tick=100 handle_tick=102 wakeup_latency=2 polling_iterations=45321 status=PASS
[METRIC] suite=wait event=filemod delay=20 run=3 trigger_tick=200 handle_tick=202 wakeup_latency=2 polling_iterations=42111 status=PASS
```

图表建议：

- 柱状图：polling_iterations；
- CDF 或折线图：wakeup_latency；
- 表格：MESSAGE 与 FILEMOD 的 median/P95 latency。

答辩结论：

```text
AgentOS 事件等待避免用户态空转，同时保持较低唤醒延迟。
```

## 8. 实验五：调度响应性与公平性

目标：证明事件感知调度和软预算机制能让关键 Agent 更快响应，同时低优先级 Agent 不会长期饥饿。

修改位置：

```text
user/schedmetric.c
```

当前代码中新增独立量化程序 `schedmetric`，不改动原有 `agentperftest`：

```text
schedmetric        # 快速验证：agents=2, runtime=200
schedmetric small  # 小规模采数：agents=2/4, runtime=300
schedmetric full   # 正式采数：agents=2/4/8, runtime=500
```

第一版变量：

```text
agents = 2 / 4 / 8
priority = low / normal / high
runtime = 500 ticks
```

暂不做 16 Agent，避免 QEMU 抖动和 xv6 进程资源影响结果。

记录字段：

```text
suite=scheduler
agents
pid
priority
quota
dispatch
max_wait
p50_wait
p95_wait
fairness
status
```

Jain 公平性指数：

```text
fairness = (sum(dispatch_i) * sum(dispatch_i)) / (n * sum(dispatch_i * dispatch_i))
```

输出示例：

```text
[METRIC] suite=scheduler agents=4 pid=7 priority=8 quota=8 dispatch=103 p95_wait=4 max_wait=9 status=PASS
[METRIC] suite=scheduler agents=4 fairness=0.91 high_dispatch=103 low_dispatch=42 status=PASS
```

图表建议：

- 柱状图：不同优先级 Agent 的 dispatch；
- 折线图：Agent 数量增加时 P95 wait；
- 表格：fairness 指数。

答辩结论：

```text
高优先级 Agent 获得更快响应；低优先级 Agent 仍有调度机会，软预算避免永久饥饿。
```

## 9. 实验六：FIFO mailbox 并发压力

目标：证明 FIFO mailbox 在并发消息下不会覆盖旧消息，并能明确统计 accepted、received、busy/dropped。

当前新增：

```text
user/agentmailbench.c
```

运行方式：

```text
mailbench        # 快速验证：senders=1, messages=10, consumer=fast
mailbench small  # 小规模采数：senders=1/2, messages=10/30
mailbench full   # 正式采数：senders=1/2/4, messages=10/30
```

第一版变量：

```text
senders = 1 / 2 / 4
messages_per_sender = 10 / 30
consumer = fast / slow
capacity = 8
```

说明：当前 mailbox 容量是内核常量，第一版只记录 `capacity=8`，不为了实验修改内核容量。

每条消息包含：

```text
from_pid
sender_id
producer_sequence
```

记录字段：

```text
suite=mailbox
senders
messages_per_sender
capacity
sent
accepted
received
busy
order_errors
duplicates
wrong_sender
status
```

输出示例：

```text
[METRIC] suite=mailbox senders=4 messages=30 capacity=8 sent=120 accepted=80 received=80 busy=40 order_errors=0 duplicates=0 wrong_sender=0 status=PASS
```

图表建议：

- 堆叠柱状图：received + busy；
- 表格：order_errors、duplicates、wrong_sender 是否为 0。

答辩结论：

```text
高并发消息不会覆盖或串包；队列满时返回 BUSY，并保留已有 FIFO 消息。
```

## 10. 宿主机脚本设计

不使用 Python 画图，但仍建议用 shell/awk/sed 做日志抽取。

当前新增：

```text
tools/perf/run_agentos_perf.sh
tools/perf/run_dual_codelab.sh
tools/perf/metrics_to_csv.sh
```

运行方式：

```text
tools/perf/run_agentos_perf.sh quick   # AgentOS 量化冒烟采集
tools/perf/run_agentos_perf.sh small   # AgentOS 小规模采集
tools/perf/run_agentos_perf.sh full    # AgentOS 正式采集
tools/perf/run_dual_codelab.sh         # baseline plain 与 AgentOS CodeLab 对照
```

如果某次实验在你的机器上运行较慢，可以调整宿主机等待时间：

```text
PERF_RUN_SECONDS=300 tools/perf/run_agentos_perf.sh quick
PERF_RUN_SECONDS=300 PERF_TIMEOUT=420 tools/perf/run_dual_codelab.sh
```

脚本会在 `dual-results/run_YYYYMMDD_HHMMSS/` 下生成：

```text
raw/  原始 QEMU 日志
csv/  从 [METRIC] 行转换出的 CSV
```

### 10.1 `metrics_to_csv.sh`

作用：

```text
输入 raw log
过滤 [METRIC]
输出 key=value 形式的 CSV
```

已用 awk 实现，不依赖 Python。

输出：

```csv
suite,case,target,mode,event,phase,agents,senders,messages,consumer,capacity,normal_capacity,files,run,delay,ticks,scanned,plan_scanned,full_scanned,matches,total_ticks,files_scanned,tool_calls,query_file_calls,syscalls,polling_loops,idle_ticks,duplicate_queries,context_hit,context_hits,cache_hit,cache_hits,dispatch,p50_wait,p95_wait,max_wait,fairness,high_dispatch,low_dispatch,accepted,received,busy,order_errors,duplicates,wrong_sender,wakeup_latency,trigger_tick,handle_tick,cpu_ticks,status
agentfs,,,index,,,,,,,,,100,3,,12,8,8,50,2,,,,,,,,,,,,,,,,,,,,,,,,,,,,PASS
```

也可以单独转换已有日志：

```text
tools/perf/metrics_to_csv.sh raw.log > metrics.csv
cat raw.log | tools/perf/metrics_to_csv.sh > metrics.csv
```

### 10.2 画图方式

可选方案：

1. WPS/Excel/LibreOffice  
   直接导入 CSV，手工生成柱状图、折线图和堆叠柱状图。

2. gnuplot  
   使用 `.plt` 脚本读取 CSV 或整理后的 `.dat` 文件生成 PNG/SVG。

3. R / ggplot2  
   如果后续愿意使用 R，可以用 R 脚本画图，但不是本方案默认要求。

4. 手工汇总表格  
   对于 PASS/FAIL 矩阵、fairness 指数、order_errors 等，可以直接用 Markdown 表格或 PPT 表格展示。

推荐当前阶段：

```text
先用 metrics_to_csv.sh 生成 CSV；
再用 WPS/Excel 手工画 5 张核心图；
如果后续需要自动化，再补 gnuplot 脚本。
```

## 11. 推荐实施顺序

第一步：实现 baseline plain CodeLab。

```text
baseline-xv6/repo/*
baseline-xv6/user/plainlab.c
baseline-xv6/Makefile
```

第二步：让 AgentOS 侧 CodeLab 输出 `[METRIC] suite=dual target=agentos ...`。

第三步：改 `agentfsbench` 输出 AgentFS 规模曲线指标。

第四步：改 `agentinnovationtest` 输出 shared cache 三阶段指标。

第五步：改 `agentperftest` 输出 wait 与 scheduler 指标。

第六步：新增 `agentmailbench` 输出 mailbox 压力指标。

第七步：新增 `metrics_to_csv.sh`，先能把日志整理成 CSV。

第八步：用 WPS/Excel/LibreOffice 画图并整理到 PPT。

## 12. 最终答辩建议保留的图

如果 PPT 空间有限，建议保留 5 张图：

1. AgentFS scan/index/cache 的 P95 ticks 随文件数变化；
2. shared cache 首次查询、命中、失效后三阶段扫描数；
3. plain xv6 与 AgentOS CodeLab 的 total_ticks/files_scanned/polling_loops 对照；
4. `agent_wait` 与 polling 的空转次数和唤醒延迟；
5. FIFO mailbox 并发下 received/busy/order_errors。

调度公平性可以用表格展示：

```text
agents | high_dispatch | low_dispatch | fairness | p95_wait | max_wait
```

文件租约与故障恢复本阶段暂时只展示功能测试：

```text
agentlease
codelabfail
```

不纳入本轮量化图表。
