# <u>四个</u> proj61 项目的决赛借鉴与 AgentOS-AAA 改进建议

## 1. 文档目的

本文根据：

- `AgentOS-coresurgen`
- `AgentOS-high`
- `AgentOS-uCore`
- `AgentOS-processnd`
- `AgentOS-AAA`
- `proj61赛题说明.md`
- `分析.md`

总结其他四个 proj61 实现中值得 AgentOS-AAA 在决赛前借鉴的设计，并分析这些项目的主要缺点，以及 AgentOS-AAA 可以针对这些缺点做出的改进。

本文不比较底层教学 OS 的基础内核能力，只讨论赛题要求的 Agent 进程、Tool Call、Context Path、AgentFS、Agent Loop、多 Agent 协作和决赛验收。

结论基于代码与文档静态分析；没有把对方文档中的所有“已完成”声明都等同于本次提交已经通过完整端到端运行。

---

## 先看这里：推荐修改点总表（按价值从高到低）

下面只按“对 AgentOS-AAA 的技术价值和答辩说服力”排序，不考虑实现时间。具体验证实验统一放在下一节。

任务标注含义：任务一为 Agent 进程与地址空间，任务二为结构化 Tool Call，任务三为 Context Path，任务四为 AgentFS，任务五为 Agent Loop 与调度，任务六为综合场景。一个改进同时影响多个赛题任务时会并列标注。

1. ==**把单消息槽升级为有界 FIFO 消息/事件邮箱。**==（任务二、任务五）  
   借鉴 AgentOS-high 和 AgentOS-uCore，解决 AAA 以及 coresurgen 的连续消息覆盖问题，也避免 processnd 的 bitmask 合并同类事件。消息增加 `from_pid/type/length/sequence/request_id/payload`，PCB 保存 `head/tail/count/dropped`，满队列返回 `BUSY` 而不是覆盖。

2. ==**增加基于 inode 版本的文件编辑租约。**==（任务四、任务六）  
   借鉴 AgentOS-uCore，解决多个 Patch-Agent 同时修改同一文件时相互覆盖、测试版本与审核版本不一致的问题。实现 `edit_begin/commit/abort`、`lease_id/base_version/expiry_tick` 和 `CONFLICT/STALE`。

3. ==**增加轻量 capability 权限模型。**==（任务一、任务二、任务五）  （不同 Agent 角色之间的操作权限隔离。）
   借鉴 AgentOS-uCore，解决 processnd 任意进程可伪造事件、AAA 权限分散且主要依赖 Agent type/group 的问题。给 Tool Router 中每个工具配置 `required_cap`，让不同角色使用最小权限。

4. ==**建立统一的安全与系统完整性闭环。**==（任务一～任务六）  
   综合借鉴四个仓库，尤其是 AgentOS-uCore 的权限、可信记录和生命周期治理，以及 AgentOS-high 的 guard page、固定 ABI 和回归纪律。所有有副作用的 Agent 操作统一遵守 `Validate → Authorize → Reserve → Execute → Commit → Publish → Record → Cleanup`，并覆盖 create/fork/exec/exit/reap、PID 复用、资源耗尽和失败回滚。

6. ==**建立“用户态快速 Context + 内核可信摘要”的双层 Context。**==（任务一、任务三）  
   借鉴 AgentOS-uCore 的 shadow/hash chain，同时避免其完整 shadow 和 sidecar 的巨大内存开销。AAA 保留用户态 Context 作为工作记忆，内核只保存 `sequence/status/request_hash/result_hash/previous_hash`。

   > 用户态 Context 读取快，但用户程序自己可以改。
   >  所以 uCore 项目在内核里再保存一份关键记录，作为“真实版本”
   
7. ==**固定 Context ABI，并增加 guard page 和一致性版本。**==（任务一、任务三）  
   借鉴 AgentOS-high 和 processnd。Context header 明确 `magic/version/header_size/node_size/generation/sequence/dropped`，Context 区边界增加 guard page，用户态通过 generation 获得一致快照。

   > 在你的 AgentOS 中，Context ABI 就是：
   >
   > > **内核写入 Agent Context 区时使用什么结构，用户态 Agent 应该按照什么结构读取。**
   > 
   > 把现在隐式约定的 Context 格式正式固定下来，方便后续修改和答辩说明。

8. ==**让单 Agent Context 复用与跨 Agent shared cache 复用形成明确闭环。**==（任务三、任务四、任务六）  
   借鉴 coresurgen 的<u>重复任务</u>设计，明确区分短期 Context 和跨进程共享查询结果，并让相同任务能够跳过已经完成的工具调用。

9. ==**建立原生 xv6 与 AgentOS-AAA 的双目标对照实验。**==（任务六，兼作任务一～任务五的综合验收）  
   借鉴 AgentOS-uCore 的双目标科研平台，让同一 CodeLab 工作负载分别使用普通 xv6 机制和 AgentOS 内核机制完成。先验证输入、补丁、测试和审核结果等价，再比较索引、Context/cache、事件等待、并发冲突、权限和因果证据。

10. ==**建立一键验收脚本和 Requirements Traceability。**==（任务一～任务六）  
       借鉴 coresurgen 的逐任务脚本、processnd 的 PASS 输出和 AgentOS-uCore 的需求追踪/CI，统一完成编译、QEMU 启动、超时、panic 检测、测试执行、日志保存和指标汇总。

12. ==**把硬 quota 跳过改成软预算调度，并输出调度原因。**==（任务五）  
    借鉴 AgentOS-uCore 的 `ready_age + vruntime + budget penalty + MAX_AGENT_BURST`：budget 用尽后降低分数而不是彻底禁止运行，连续按高分调度达到上限后强制选择等待最久的进程。这样既保留事件响应能力，也避免 AAA 当前预算无法补充和 coresurgen/processnd 严格优先级造成的永久饥饿；同时输出 base priority、event bonus、aging、vruntime、budget penalty 和最终 score。

    > 感觉就是优化调度的算法

14. ==**为 Tool Call 增加 schema、<u>版本和统一错误模型</u>。**==（任务二）  
    借鉴 coresurgen 的 typed KV、AgentOS-uCore 的 typed/id-based ABI、high/processnd 的清晰工具列表。保留 AAA 的字符串 KV 和动态工具，让 `tool_list` 返回参数类型、必填项、权限、结果字段和错误码，并预留 `tool_call_v2`。

16. ==**强化真实 FILEMOD 的精确性。**==（任务四、任务五）  
    针对 coresurgen 的全局文件事件、processnd 的 FILE_WRITE 未接入写路径、high 的文件名监听局限，保持 AAA 的 `dev/inum` 监听，并完善 rename、同名 inode 和非目标文件语义。

    > 你的 `dev/inum` 监听方向已经是正确的，不需要推倒重做。主要补充几个测试即可：
    >
    > ```
    > 1. 修改目标文件 → 必须收到 FILEMOD
    > 2. 修改非目标文件 → 不能收到 FILEMOD
    > 3. 两个目录下同名文件 → 不能混淆
    > 4. 同一 inode 的不同路径 → 应视为同一个文件
    > 5. rename 后写入 → 若实现 rename，应继续正确通知
    > ```
    >
    > 所以这条建议本质上不是新增一个大功能，而是：
    >
    > > **证明你的 FILEMOD 是从真实 `write` 路径触发，并且精确关联到目标 inode，不会误报或漏报。**

17. ==**为 `agent_wait` 增加 timeout、cancel 和无唤醒源保护。**==（任务五）  
    借鉴 AgentOS-high 的 `NO_WAKE_SOURCE`、AgentOS-uCore 的 timeout/cancel 和 processnd 的定时等待接口，避免 Agent 在没有 heartbeat、watch、消息和 timeout 时不可诊断地永久休眠；timer 使用 `identity_generation + wait_generation` 防止旧超时重复唤醒。

18. ==**？提供 `agent_spawn` 抽象并写清 fork/exec 继承语义。**==（任务一）  
    借鉴 coresurgen 的路径式 Agent 创建和 processnd 明确的 fork 继承规则。用用户态 `fork + exec + agent_create` 封装“创建新 Agent”，并规定 Context、身份、capability、heartbeat、watch 和动态工具的继承或清理规则。

19. ==**统一日志字段并建立轻量 timeline。**==（任务三、任务五、任务六）  
    借鉴 AgentOS-uCore 的 timeline，但不复制完整内核查询子系统。所有模块统一输出 `tick/span/request/agent/role/action/status/cause`，Planner 按 span 汇总。

22. ==**实现 Tool Call batch。**==（任务二）  
    借鉴 AgentOS-uCore 的批处理路径，一次提交多个工具请求并保留每项独立状态，减少高频批量查询中的 syscall。

23. ==**？根据数据选择 Context 节点布局。**==（任务一、任务三）  
    借鉴 high/processnd 的固定槽位，同时保留 AAA 变长节点的空间优势。比较变长搬移、固定槽位、分段环和 byte ring + descriptor ring，再决定是否重构。

24. ==**把 heartbeat 扫描升级为 deadline heap 或时间轮。**==（任务五）  
    针对 processnd 每 tick 扫描全部 Agent 的扩展性问题，用 deadline 数据结构降低 Agent 数量增大后的 tick 开销。

第 4 项是第 1～3、5～7、13～18 项的横向约束：这些能力不能只作为彼此独立的功能实现，而应共同保证验证前无副作用、运行中最小权限、提交后状态一致、退出后无残留。具体设计见第 3.9 节。

---

## 功能验证实验应该如何改进

以下清单已经扣除了 `user/agenttest.c`、`agentlooptest.c`、`agentinnovationtest.c`、`agentfsbench.c` 和 `agentperftest.c` 当前已有的测试，只列仍需补充的内容。

### <u>1. Context 边界与可信性：扩展 `agenttest`</u>

- 访问 Context guard page 时能够被可靠拦截；
- fork/exec 后 Context、Agent 类型、quota、watch 等状态按文档继承或清理；
- 构造非法节点长度、损坏 sequence/generation，读取方能够拒绝；
- 篡改用户态 Context 镜像后，内核可信摘要校验失败；
- rollback 后，旧 sequence 不可再次作为有效复用来源。

### <u>2. FIFO 邮箱与事件异常路径：扩展 `agentlooptest`</u>

- 连续发送多条编号消息，断言 FIFO 顺序和每条消息的 `from_pid`；
- 多发送者并发写入同一邮箱，断言消息不覆盖、不串包；
- 超过邮箱容量时验证 `BUSY/dropped` 和既有消息完整性；
- 消息洪泛时，FILEMOD、HEARTBEAT、CANCEL 仍能在有界时间内处理；
- 没有任何唤醒源时返回 `NO_WAKE_SOURCE`，目标 Agent 退出后发送失败且无悬空状态；
- 修改未监听 inode 不会误唤醒，并明确验证 rename/unlink 后的监听语义。

### 3. 权限与拒绝后无副作用：新增 `agentcaptest`

为 Planner、Retriever、Patch、Reviewer 和普通进程建立 `query/read/patch/send/watch/register/sched` 的允许—拒绝矩阵。每个拒绝用例除检查错误码外，还必须断言：

- 文件内容和版本未改变；
- 消息未入队；
- watch 和调度参数未改变；
- 动态工具表未改变；
- 生成包含调用者、目标和拒绝原因的审计记录。

### 4. 文件并发修改：新增 `agentleasetest`

- 两个 Agent 竞争同一 inode 时只能有一个成功，不同 inode 可并行；
- 非 owner 不能 commit/abort，Agent 退出或 lease 超时后资源可回收；
- base version 已变化时旧 commit 返回 `STALE`；
- 成功 commit 只增加一次版本，并触发 FILEMOD 和 shared cache 失效；
- abort 不改变文件内容、版本和缓存状态。

### <u>5. AgentFS 统计证据：扩展 `agentfsbench`</u>

- 文件数 `10/50/100/200` 及均匀、热点、缺失索引键三类分布；
- cold cache、warm cache、失效后重查三种状态；
- 每组多轮运行并报告 median、P95 和 cache hit rate；
- 单独测量索引 build/rebuild 成本；
- 验证 group/private 查询结果及缓存不会被无权限 Agent 复用；
- 保证 scan/index/cache 三条路径输入、过滤条件和结果集合完全相同。

### 6. 调度公平性与压力：新增 `agentschedbench`

- aging 前后低优先级 Agent 的最大和 P95 等待 tick；
- quota 用尽后的降级/让出行为，以及高负载 Agent 不能长期独占 CPU；
- `2/4/8` 个 Agent 与不同优先级、quota 组合下的运行次数和唤醒延迟；
- 多核配置下没有重复入队、丢失唤醒、panic 或死锁；
- 长时间压力运行后，每个可运行 Agent 都获得过调度。

### 7. 动态工具异常生命周期：扩展 `agentinnovationtest`

- 注册信息能由 `tool_list`/describe 查询到，并校验 schema、owner、version、flags；
- 同名注册冲突、非授权注册和非授权调用；
- 多请求 FIFO、队列满 `BUSY` 和请求 timeout；
- 调用者中途退出时请求被清理，服务异常退出时工具自动注销；
- timeout 后迟到的 reply 返回 `STALE_REQUEST`；
- 动态 Tool Call 形成的 Context 节点包含正确的 request/span ID。

### 8. 真正减少重复工作的 Context 复用：扩展 `agentinnovationtest`

- 同一 Agent 连续执行相同任务，第二次直接复用 Context；
- 修改文件内容后旧 Context 结果失效；
- rollback 后相关节点不能继续命中；
- private/group Context 不能被越权 Agent 复用；
- 对比两次执行的 `tool_calls` 和 ticks，输出 `context_hit`、`reused_sequence`、`skipped_tool_calls`、`saved_ticks`。

### 9. 因果链和故障恢复：扩展 CodeLab 测试

- 检查 Retriever、Patch、FILEMOD、Test、Reviewer 和 Planner summary 共享正确的 `span_id/request_id/cause_id` 链；
- 检查 Test 和 Reviewer 处理的是 Patch 实际提交的同一文件版本；
- 在 Retriever、Patch 或 Tool Service 中途退出时，系统给出确定的失败、清理或替代路径；
- 从最终 summary 能反向定位到真实工具请求、文件版本和测试结果。

### 10. 增量实验的统一输出

新增用例统一输出机器可解析的 case 和统计结果：

```text
[TEST] suite=agentmailbox case=overflow status=PASS
[METRIC] sent=20 received=8 dropped=12 order_errors=0
[METRIC] repeats=30 median=... p95=...
[SUMMARY] suite=agentmailbox pass=6 fail=0
```

一键脚本汇总各测试的 PASS/FAIL，并维护“赛题任务—测试 case—内核代码位置”映射；不重复实现各 `agent*.c` 内部已有的断言。

## 量化与可视化展示方案

功能测试回答“是否实现”，量化实验还要回答“带来了多少收益、规模扩大后是否仍然成立”。建议围绕下面七组实验生成统一 CSV，再由宿主机脚本绘图。

| 优先级 | 实验 | 主要指标 | 推荐图表 | 答辩结论 |
|---:|---|---|---|---|
| 1 | AgentFS：scan/index/cache | `scanned_count`、ticks、P50/P95、hit rate | 折线图、分组柱状图 | 文件越多，索引和缓存收益越明显 |
| 2 | Context 与 shared cache 复用 | Tool Call 数、跳过步骤、saved ticks | 首次/二次柱状图 | Context 确实减少重复工作 |
| 3 | AAA 与原生 xv6 等价对照 | 总 ticks、扫描数、轮询数、syscall 数 | 分组柱状图 | AgentOS 内核机制带来系统级收益 |
| 4 | `agent_wait` 与轮询 | 空转次数、CPU ticks、唤醒延迟、P95 | 柱状图、CDF | 事件驱动降低空转且响应及时 |
| 5 | 调度响应性与公平性 | P50/P95 延迟、最大等待、dispatch、公平指数 | CDF、折线图、热力图 | 高优先级响应快，低优先级不饥饿 |
| 6 | FIFO mailbox 压力 | sent/received/dropped、顺序错误、重复消息 | 堆叠柱状图 | 并发消息不覆盖，溢出行为明确 |
| 7 | 文件租约与故障恢复 | conflict、lost update、恢复 ticks、残留资源 | 柱状图、结果矩阵 | 并发和异常退出不会破坏系统状态 |

### 1. AgentFS 规模曲线

扩展 `agentfsbench`，固定生成：

```text
文件数：10 / 50 / 100 / 200 / 500
查询路径：scan / index / cache
缓存状态：cold / warm / invalidated
属性分布：均匀 / 热点 / 缺失索引键
```

记录：

```text
candidate_count
scanned_count
match_count
elapsed_ticks
cache_hit
index_build_ticks
median
p95
```

主图使用“文件数—P95 ticks”折线图，辅图使用“文件数—scanned_count”折线图。scan、index、cache 必须使用相同输入、过滤条件和结果集合，不能比较工作量不同的路径。

### 2. Context 与跨 Agent 缓存复用收益

分别量化两个机制：

```text
同一 Agent：
  第一次完整执行 query/read/analyse
  第二次命中 Context 并跳过已完成步骤

不同 Agent：
  Retriever 首次 query_file，cache_hit=0
  Reviewer 重复查询，cache_hit=1
  文件修改后再次查询，cache_hit=0
```

记录：

```text
context_hit
cache_hit
tool_calls
query_file_calls
read_file_calls
scanned_count
skipped_tool_calls
elapsed_ticks
saved_ticks
```

用并列柱状图比较首次执行和复用执行的 Tool Call 数、扫描数和 ticks；跨 Agent 缓存用“首次查询—命中—失效后重查”三阶段柱状图。不能只展示 `context_hit=1`，必须展示它实际减少了多少调用和时间。

### 3. AAA 与原生 xv6 的等价 CodeLab 对照

两条路径使用相同仓库初始状态、任务、目标补丁和测试：

```text
定位 todo.c
发现 delete_task 缺陷
生成并提交补丁
运行规则测试
审核结果
```

plain xv6 使用目录遍历、pipe、sleep/polling 和用户态中间状态；AAA 使用 AgentFS、Context、事件等待、动态工具和多 Agent 协作。先断言最终补丁、测试和审核结果等价，再比较：

```text
total_ticks
files_scanned
tool_calls
syscalls
polling_loops
idle_ticks
duplicate_queries
context_hits
cache_hits
```

核心图使用分组柱状图。雷达图可以作为概览，但不能替代带单位和误差线的主图。

### 4. 事件等待与轮询

设置事件在 `10/20/50` ticks 后到达，对比：

```text
while (...) 持续轮询
agent_wait 等待 MESSAGE/FILEMOD
```

记录：

```text
event_trigger_tick
event_handle_tick
wakeup_latency
polling_iterations
CPU_ticks_consumed
```

使用柱状图展示空转次数和 CPU ticks，使用 CDF 展示多轮 MESSAGE/FILEMOD 唤醒延迟。扩展 `agentperftest` 时应批量重复事件，避免单次操作小于 tick 分辨率而得到大量零值。

### 5. 调度响应性与公平性

建议建立矩阵：

```text
Agent 数：2 / 4 / 8 / 16
优先级：low / normal / high
事件：MESSAGE / FILEMOD / HEARTBEAT
负载：idle / medium / CPU-saturated
运行时间：500～2000 ticks
```

记录：

```text
dispatch_count
ready_wait_ticks
event_wakeup_latency
max_wait_ticks
p50_latency
p95_latency
normal_process_dispatch_count
forced_fifo_count
budget_penalty_count
```

同优先级 Agent 的公平性可使用 Jain 指数：

```text
fairness = (Σxi)^2 / (n × Σxi^2)
```

其中 `xi` 是每个 Agent 的 dispatch 次数，结果越接近 1 越公平。主图包括：

- Agent 数量—P95 事件延迟折线图；
- 每个 Agent dispatch 次数柱状图；
- 优先级×负载的最大等待热力图；
- 唤醒延迟 CDF。

调度实验必须同时证明事件 Agent 响应更快、普通进程仍有进展、低优先级最大等待有界，并且不存在 budget 永不恢复。

### 6. FIFO mailbox 并发压力

实验变量：

```text
发送者：1 / 2 / 4 / 8
每个发送者消息：10 / 50 / 100
邮箱容量：4 / 8 / 16
消费者：fast / slow
```

每条消息携带 `from_pid/producer_sequence/global_sequence`，统计：

```text
sent
accepted
received
dropped_or_busy
order_errors
duplicates
wrong_sender
```

使用堆叠柱状图证明 `received + dropped_or_busy = sent`，并单独展示 `order_errors=0`、`duplicates=0`。这比只打印 FIFO PASS 更有说服力。

### 7. 文件租约与故障恢复

两个 Patch-Agent 同时修改同一 inode，对比“无 lease”和“有 lease”：

```text
successful_commits
conflicts
stale_commits
lost_updates
version_increments
FILEMOD_count
cache_invalidations
```

再分别在 edit begin 后、commit 前、动态工具 recv 后和 reply 前 kill Agent，记录：

```text
remaining_leases
remaining_requests
remaining_watches
recovery_ticks
final_file_correct
workflow_can_continue
```

答辩中使用结果矩阵：

| 故障点 | 明确失败 | 无残留资源 | 文件版本正确 | 可继续执行 |
|---|---:|---:|---:|---:|
| lease 后退出 | PASS | PASS | PASS | PASS |
| Tool recv 后退出 | PASS | PASS | PASS | PASS |
| patch commit 前退出 | PASS | PASS | PASS | PASS |

### 实验统计和输出规范

所有性能实验遵守：

```text
预热次数：至少 5
正式重复：至少 30
报告：median + P95，必要时增加 min/max
控制变量：相同 QEMU CPU 数、文件系统镜像、随机种子和构建配置
小操作：批量执行 100～1000 次，避免 tick 分辨率不足
```

每次运行输出一条机器可解析记录：

```text
[METRIC] suite=agentfs case=index files=200 run=3 ticks=8 scanned=14 matches=3
[METRIC] suite=context phase=reuse tool_calls=1 context_hit=1 saved_ticks=16
[METRIC] suite=scheduler pid=7 priority=8 dispatch=103 p95_wait=4 max_wait=9
```

宿主机转换为统一 CSV：

```csv
suite,case,run,files,ticks,scanned,matches,status
agentfs,index,3,200,8,14,3,PASS
```

原始日志、CSV、绘图脚本和图片使用同一个 `run_id`，并在图注中记录 commit、QEMU 配置、重复次数和误差定义，保证图表能够从原始数据复现。

### 答辩最推荐的五张核心图

如果展示空间有限，只保留：

1. 文件数量增加时 scan/index/cache 的 P95 查询时间；
2. 首次任务与 Context 复用后的 Tool Call 数和 ticks；
3. plain xv6 与 AAA 的扫描数、轮询数和总 ticks；
4. 不同 Agent 数量下的事件 P95 延迟与最大等待时间；
5. FIFO 并发消息的 received/dropped/order errors。

五张图分别证明查询加速、记忆复用、系统价值、调度质量和通信可靠性。每张图只承担一个核心结论，不使用缺少单位、样本数和误差说明的装饰性图表。

---

## 2. 总体结论

四个项目各自最值得借鉴的方向是：

| 仓库 | 最值得借鉴的能力 | 不适合直接照搬的部分 |
|---|---|---|
| AgentOS-coresurgen | typed KV、任务逐项验收、Context 复用叙事、自动化脚本 | 单消息槽、全局文件事件、静态优先级、原始字节 Context 环 |
| AgentOS-high | 有界邮箱、Context guard page、固定 ABI、能力发现、ADR 文档 | 全目录扫描、普通 FIFO 调度、整数消息、静态工具表 |
| AgentOS-uCore | capability、可信 Context、FIFO 事件队列、文件编辑租约、trace ID、可观测性 | 过高复杂度、过大 PCB/内存开销、重型治理与元数据系统 |
| AgentOS-processnd | Agent 状态机简洁、用户态直接读 Context、固定 ABI、演示输出清晰 | 静态文件元数据、事件 bitmask、无动态工具、严格优先级 |

AgentOS-AAA 已有的核心优势包括：

- 真实 inode 文件属性与内容查询；
- 属性索引与跨 Agent shared query cache；
- 元数据版本变化后的缓存失效；
- 用户态动态工具注册、接收和回复；
- MESSAGE、FILEMOD、HEARTBEAT 事件；
- 基于 inode 的精确文件监听；
- priority、quota、event weight、aging、boost 调度；
- Planner、Retriever、Patch、Test、Reviewer 多 Agent CodeLab；
- 真实代码修改、测试和审核闭环；
- LLM Bridge。

因此决赛策略不应是继续无边界堆功能，而应是：

> 保留 AAA 的功能深度，吸收其他项目在可靠性、安全性、可解释性和验收工程上的优点，把现有能力变成评委能快速确认的证据。

---

# 3. 最值得借鉴实现的能力

## 3.1 有界 FIFO 消息/事件邮箱

### 来源

- AgentOS-high：有界邮箱；
- AgentOS-uCore：16 槽 FIFO 事件队列、来源信息和溢出统计；
- AgentOS-coresurgen、AgentOS-processnd 的单槽或 bitmask 机制则反向证明了 FIFO 的必要性。

### AAA 当前问题

AAA 的消息虽然有字符串 payload，但 PCB 中只有一个：

```c
char agent_message[AGENT_MESSAGE_MAX];
```

连续消息在 Agent 消费前到达时，后到消息会覆盖先到消息。`pending_events` 只能表明存在 MESSAGE，不能保存多条消息的顺序、数量和来源。

### 建议实现

```c
#define AGENT_MAILBOX_CAP 8

struct agent_message {
  int from_pid;
  int type;
  int length;
  uint64 sequence;
  uint64 request_id;
  char payload[AGENT_MESSAGE_MAX];
};

struct agent_mailbox {
  struct agent_message queue[AGENT_MAILBOX_CAP];
  int head;
  int tail;
  int count;
  uint64 dropped;
};
```

建议语义：

- `send_message` 将消息追加到 `tail`；
- `agent_wait` 每次从 `head` 消费一条；
- MESSAGE pending 状态由 `count > 0` 推导；
- 队列满时返回 `AGENT_TOOL_ERR_BUSY`，不覆盖旧消息；
- 在 `agent_info` 或等待结果中返回 `dropped`；
- FILEMOD 和 HEARTBEAT 不应被普通消息洪泛完全挤出，可预留一个系统事件槽，或继续让系统事件走独立 bitmask。

这个改动同时提升：

- 多 Agent 协作可靠性；
- 资源配额控制；
- 错误处理。

---

## 3.2 轻量 capability 权限模型

### 来源

AgentOS-uCore 将 Agent role 和 capability 分离，不只判断“是不是 Agent”，还判断它是否具备读取、修改、通信、监听或编排能力。

### AAA 当前问题

AAA 已有：

- Agent type；
- Agent group；
- public/group 动态工具；
- shared cache 访问限制。

但这些权限分散在具体功能里，没有统一回答：

> Retriever 为什么不能修改代码？普通 Worker 为什么不能修改调度参数？谁可以向哪个 Agent 发消息？

### 建议实现

在 PCB 中增加：

```c
uint64 agent_capabilities;
```

定义轻量权限：

```c
#define AGENT_CAP_QUERY_PROCESS  (1ULL << 0)
#define AGENT_CAP_QUERY_FILE     (1ULL << 1)
#define AGENT_CAP_READ_FILE      (1ULL << 2)
#define AGENT_CAP_PATCH_FILE     (1ULL << 3)
#define AGENT_CAP_SEND_MESSAGE   (1ULL << 4)
#define AGENT_CAP_WATCH_FILE     (1ULL << 5)
#define AGENT_CAP_REGISTER_TOOL  (1ULL << 6)
#define AGENT_CAP_SCHED_CONFIG   (1ULL << 7)
```

Tool Router 中统一检查：

```c
if((p->agent_capabilities & tool->required_cap) == 0)
  return AGENT_TOOL_ERR_PERMISSION;
```

CodeLab 角色建议：

| 角色 | 最小权限 |
|---|---|
| Planner | 查询、通信、创建/编排、调度 |
| Retriever | query_file、read_file、通信 |
| Patch | read_file、patch_file、通信 |
| Test | 调用测试工具、通信 |
| Reviewer | read、diff、watch、通信 |
| Tool-Service | register、recv、reply |

这使 AAA 不只是“能执行工具”，还具备 Agent 场景下的最小权限控制。

---

## 3.3 Context 固定 ABI、guard page 和可信摘要

### 来源

- AgentOS-high：guard page、固定 Context ABI、单调 sequence；
- AgentOS-processnd：用户态直接读取 Context；
- AgentOS-uCore：用户 mirror + 内核 shadow + hash chain。

### AAA 当前问题

AAA 的 Context 已经放在用户态区域，并由内核维护元数据和自动写入工具记录。这符合赛题“策略性高频数据在用户态”的要求。

但仍有三个改进空间：

1. 用户态能修改 Context 内容，所以它适合作为工作记忆，不适合作为可信审计记录；
2. ABI 虽有 header，但版本、节点尺寸、sequence 和一致性说明还可以更明确；
3. Context 区边界最好有 guard page，增强安全性和地址空间设计说服力。

### 建议实现

Context header 增加或明确：

```c
uint32 magic;
uint32 version;
uint32 header_size;
uint32 node_size;
uint64 generation;
uint64 first_sequence;
uint64 next_sequence;
uint64 node_count;
uint64 dropped_nodes;
```

Context node 增加：

```c
uint64 sequence;
uint64 request_id;
uint64 span_id;
uint64 cause_sequence;
int status;
```

内核不必复制 AgentOS-uCore 的完整 shadow。可以只维护一个小型可信摘要环：

```c
struct agent_context_digest {
  uint64 sequence;
  uint64 request_id;
  uint64 request_hash;
  uint64 result_hash;
  uint64 previous_hash;
  int status;
};
```

地址空间布局建议：

```text
Agent Context 用户可读写区
Guard Page（不映射）
高地址保留区域
```

直接读取时使用 `generation` 校验：

```text
读 generation A
读节点
读 generation B
A == B 才视为一致快照
```

---

## 3.4 统一 `version + request_id + span_id + cause_id`

### 来源

AgentOS-uCore 的工具、事件、Context、timeline 都有稳定关联 ID。

### AAA 当前问题

AAA 的 CodeLab 已经有完整因果流程，但关联关系主要存在于：

- 字符串消息；
- 不同角色的打印日志；
- Context 文本；
- Planner 最终 summary。

评委很难仅靠日志确认：

```text
哪次 query 导致了哪次 patch，
哪次 patch 触发了哪次 FILEMOD，
哪次测试结果对应哪次审核。
```

### 建议实现

不要重写整个 Tool Call ABI，先兼容性地增加：

```c
uint32 version;
uint64 request_id;
uint64 span_id;
uint64 cause_id;
```

字段贯穿：

- tool request/response；
- 动态工具转发；
- agent_message；
- Context node；
- FILEMOD 事件；
- 日志输出。

统一日志示例：

```text
[TRACE] span=42 req=100 role=retriever action=query_file status=OK
[TRACE] span=42 req=101 cause=100 role=patch action=patch_file status=OK
[TRACE] span=42 req=102 cause=101 role=reviewer event=FILEMOD
[TRACE] span=42 req=103 cause=101 role=test action=run_rule_test status=PASS
```

这是一种轻量 provenance，不需要实现 AgentOS-uCore 的完整 timeline 查询系统。

---

## 3.5 文件编辑租约与版本冲突检查

### 来源

AgentOS-uCore 的文件编辑租约直接解决多个代码 Agent 并发修改同一文件的问题。

### 为什么适合 AAA

AAA 的 CodeLab 是真实代码修复场景。当前 Patch-Agent 可以修改文件，但如果两个 Patch-Agent 同时基于旧内容修改同一 inode，可能出现：

- 补丁相互覆盖；
- Reviewer 审核的不是 Test-Agent 测试的版本；
- 旧版本 Agent 在新版本上错误提交。

### 建议实现

简化版租约表：

```c
struct agent_edit_lease {
  int used;
  uint dev;
  uint inum;
  int owner_pid;
  uint64 lease_id;
  uint64 base_version;
  uint64 expiry_tick;
};
```

接口：

```text
edit_begin(path)
  -> lease_id, base_version

edit_commit(lease_id, expected_version)
  -> new_version | STALE | CONFLICT

edit_abort(lease_id)
```

语义：

- 同一 inode 同时只有一个有效写租约；
- 冲突立即返回 `CONFLICT`，不允许 Agent 持有其他资源继续阻塞等待文件；
- commit 时验证 metadata/content version；
- Agent 退出时回收租约；
- 超时租约自动失效；
- FILEMOD 和 shared query cache invalidation 在 commit 后触发。

这是最契合“多 Agent 代码协作”的系统级创新之一。

---

## 3.6 让 Context 复用产生明确收益

### 来源

AgentOS-coresurgen 的优势不在 Context 底层结构，而在它明确演示：

```text
第一次处理任务 → 查询、推理、写 Context
第二次处理相同任务 → Context 命中、复用结果
```

### AAA 当前情况

AAA 已经实现：

- 单 Agent Context Path；
- 工具调用自动记录；
- 跨 Agent shared query cache。

但是需要明确区分：

```text
Context Path = 单 Agent 的短期推理路径
AgentFS/仓库 = 长期外部记忆
shared query cache = 多 Agent 的共享工作集
```

### 建议实现

同一 Agent 再次处理等价请求时，应先匹配有效 Context 节点并复用结果；另一个 Agent 执行相同 AgentFS 查询时，则通过 shared query cache 复用查询结果。二者分别承担单 Agent 短期工作记忆和跨 Agent 共享工作集的职责，且都必须受请求参数、权限和文件版本约束。

---

## 3.7 一键决赛验收与 Requirements Traceability

### 来源

- AgentOS-coresurgen：一任务一 demo、一文档一脚本；
- AgentOS-uCore：需求追踪、CI 预算、plain/AgentOS 对照；
- AgentOS-processnd：固定 PASS 编号和清晰终端输出；
- AgentOS-high：ADR 记录设计决策。

### AAA 当前问题

AAA 的测试和场景很多，但入口比较分散。功能丰富反而可能使评委在短时间内无法判断：

- 六项任务分别由什么代码实现；
- 用什么测试验证；
- 最终输出在哪里；
- 性能收益来自什么；
- 失败时是哪一层出错。

### 建议实现

增加统一入口：

```bash
./tools/run_final_demo.sh
```

自动完成：

1. 编译；
2. 启动 QEMU；
3. 运行基础任务测试；
4. 运行 AgentFS benchmark；
5. 运行 mailbox、调度和权限测试；
6. 运行 CodeLab；
7. 检查 panic、超时和 FAIL；
8. 保存原始日志；
9. 汇总关键指标。

统一输出前缀：

```text
[CREATE]
[TOOL]
[CONTEXT]
[AGENTFS]
[MAILBOX]
[AUTH]
[WAKEUP]
[SCHED]
[LLM]
[SUMMARY]
[PASS]
```

增加需求追踪表：

| 赛题要求 | 核心代码 | 测试程序 | 现场证据 |
|---|---|---|---|
| Agent 创建与 Context 区 | `kernel/agent.c` | `agenttest` | `[CREATE]` |
| 结构化工具调用 | `kernel/agent_tool.c` | `agenttooltest` | `[TOOL]` |
| Context push/query/rollback | `kernel/agent_context.c` | `agenttest` | `[CONTEXT]` |
| 属性查询与索引 | `kernel/agent_fs.c` | `agentfsbench` | `[AGENTFS]` |
| 心跳、事件、调度 | `kernel/agent_loop.c` | `agentlooptest` | `[WAKEUP]/[SCHED]` |
| 多 Agent 场景 | `user/*_agent.c` | CodeLab | `[SUMMARY]` |

---

## 3.8 AAA 与原生 xv6 的双目标对照实验

### 来源与目标

借鉴 AgentOS-uCore 的“双目标科研平台”方法：不是只运行 AgentOS 测试后说明功能通过，而是让**同一个多 Agent 工作负载**分别运行在普通 OS 对照目标和 AgentOS 增强目标上，直接回答：

1. 原生 xv6 依靠普通进程、文件、pipe 和轮询能完成什么；
2. 加入 AgentOS 内核机制后，同一个任务在哪些方面更高效、可靠、安全和可追踪；
3. AgentOS 是否保持了普通路径的最终业务结果，而不是用不同任务制造优势。

AAA 可以建立两个目标：

```text
目标 A：baseline-xv6
  原生 xv6 内核 + 运行工作负载必需的通用修复
  不含 Agent syscall、Context、AgentFS、事件队列和 Agent 调度

目标 B：AgentOS-AAA
  当前 AAA 内核
  含 Agent Context、Tool Call、AgentFS、shared cache、
  MESSAGE/FILEMOD/HEARTBEAT、动态工具和 Agent 调度
```

对照目标必须明确记录所用 xv6 基线 commit。若某项通用 bugfix 同时影响两边，应同步进入两个目标；Agent 专属代码只能进入 AAA。这样比较的是 AgentOS 增量，而不是两个无关内核的差异。

### 目录与构建建议

推荐保留真正独立的 baseline：

```text
AgentOS-AAA/
  kernel/                    AgentOS 增强内核
  user/                      AgentOS 用户程序
  baseline_xv6/
    kernel/                  无 AgentOS 扩展的 xv6 对照内核
    user/                    plain CodeLab 用户程序
  dual_workload/
    common/                  相同任务、输入、断言和结果格式
    plain_backend.c          xv6 普通机制适配
    agentos_backend.c        AgentOS 机制适配
  tools/
    run_dual_codelab.sh
    compare_dual_results.py
```

如果复制完整 xv6 会增加同步负担，也可以从同一源码树构建两个配置：

```text
make TARGET=plain
make TARGET=agentos
```

但必须确保 `TARGET=plain` 的符号表、系统调用表和运行日志中确实不存在 AgentOS 接口。相比仅在 AAA 用户程序中“不调用 Agent syscall”，独立内核目标的对照说服力更强。

### 使用同一套 CodeLab 工作负载

两边使用完全相同的初始状态：

- 相同的 `repo/todo.c`；
- 相同的缺陷；
- 相同的文件数量和目录结构；
- 相同的任务描述；
- 相同的角色和处理阶段；
- 相同的目标补丁；
- 相同的测试规则；
- 相同的最终审核标准。

任务流程统一为：

```text
任务输入
  → 查找与 delete_task 相关的文件
  → 读取目标代码
  → 定位缺陷
  → 生成并提交补丁
  → 运行规则测试
  → Reviewer 审核
  → 输出结果和证据
```

两个目标的最终正确性条件必须相同：

```text
目标文件 hash 相同
补丁内容相同
测试结果相同
Reviewer 结论相同
任务完成状态相同
```

只有先证明业务结果等价，后续性能、可靠性和可观测性比较才成立。

### plain xv6 路径如何实现

plain 目标不能偷偷复用 AAA 的 Agent 机制，应使用传统 OS 能力：

| Agent 需求 | plain xv6 实现 |
|---|---|
| 角色进程 | `fork/exec/wait` |
| 任务通信 | pipe 或普通状态文件 |
| 文件查询 | 遍历目录、打开文件、读取内容并匹配 |
| 工具调用 | 用户态函数或独立服务进程协议 |
| 上下文历史 | 用户态日志文件或进程内数组 |
| 重复查询共享 | 各进程独立查询，或显式用户态共享文件 |
| 等待任务 | sleep + 轮询 pipe/文件状态 |
| 文件修改通知 | 周期检查文件内容/hash/mtime 替代指标 |
| 补丁并发控制 | 用户态 lock 文件和版本字段 |
| 轨迹记录 | 各进程写日志，最后由 Planner 拼接 |
| 权限 | 用户态角色约定 |

plain 路径的目的不是故意写得很差，而是使用合理的传统 xv6 用户态方案完成同一任务。

### AgentOS-AAA 路径如何实现

AgentOS 目标使用 AAA 的原生机制：

| Agent 需求 | AgentOS-AAA 实现 |
|---|---|
| 角色进程 | Agent type/role 与 Agent 创建接口 |
| 任务通信 | 有界 FIFO mailbox |
| 文件查询 | AgentFS 属性/内容索引 |
| 工具调用 | `tool_call` 与动态工具 |
| 上下文历史 | Agent Context Path |
| 重复查询共享 | Context reuse + shared query cache |
| 等待任务 | MESSAGE/FILEMOD/HEARTBEAT 阻塞等待 |
| 文件修改通知 | 基于 `dev/inum` 的 FILEMOD |
| 补丁并发控制 | inode version + edit lease |
| 轨迹记录 | request/span/cause + Context/timeline |
| 权限 | capability + group |

### 避免两套业务代码失去可比性

最好不要分别维护两个完全不同的 CodeLab。可以定义统一 backend：

```c
struct workflow_backend {
  int (*find_files)(const char *query, struct result *out);
  int (*send_task)(int target, const char *message);
  int (*wait_task)(struct event *event);
  int (*record_context)(const char *request, const char *result);
  int (*edit_begin)(const char *path, struct edit_state *state);
  int (*edit_commit)(struct edit_state *state);
  int (*trace)(struct trace_record *record);
};
```

工作流主体、输入和断言共用，只替换：

```text
plain_backend
agentos_backend
```

如果两边必须使用不同源文件，应建立结构检查：

- 两边工作流阶段数量相同；
- 输入数据生成器相同；
- 结果 schema 相同；
- plain 已有成功字段必须被 AgentOS 保留；
- 仅允许 backend 适配文件不同；
- 禁止 baseline 引用 AgentOS syscall 和头文件。

### 应比较的四类结论

#### 1. 功能非退化

AgentOS 必须保留 plain xv6 的全部正确结果：

```text
plain_success_records ⊆ agentos_success_records
plain_final_result == agentos_final_result
```

AgentOS 可以增加 Context、cache、事件、权限和 provenance 证据，但不能少执行任务、降低测试标准或省略 Reviewer。

#### 2. 性能与系统调用成本

记录：

| 指标 | plain xv6 | AgentOS-AAA |
|---|---:|---:|
| 总完成 ticks |  |  |
| syscall 数 |  |  |
| 文件 open/read 次数 |  |  |
| 实际扫描 inode 数 |  |  |
| 重复工具调用数 |  |  |
| polling 循环次数 |  |  |
| sleep/wakeup 次数 |  |  |
| 消息到执行的延迟 |  |  |
| 内核/用户态复制字节数 |  |  |

性能结果至少区分：

- 首次运行；
- 同一 Agent 第二次运行；
- 另一 Agent 重复查询；
- 文件修改导致缓存失效后的运行。

这样可以分别展示索引、Context 复用和跨 Agent shared cache 的收益。

#### 3. 可靠性与并发正确性

比较相同故障注入：

| 场景 | plain xv6 | AgentOS-AAA |
|---|---|---|
| 连续发送多条任务 | pipe/文件协议自行处理 | FIFO 顺序与 dropped |
| 接收者速度慢 | 可能阻塞或积压 | 有界队列和来源限制 |
| 两个 Patch 同时修改 | 用户态 lock/version | 内核 lease + conflict |
| 使用旧版本提交 | 用户态检查 | 内核 `STALE` |
| Worker 中途退出 | Planner 超时和清理 | 服务注销、队列/租约回收 |
| 修改非目标文件 | 轮询可能多做检查 | inode 精确 FILEMOD |

这里要比较两边的合理实现，而不是故意让 plain 目标缺少所有错误处理。AgentOS 的优势应来自内核统一强制和更少的用户态重复代码。

#### 4. 安全与可追踪性

对两个目标执行：

- Retriever 尝试修改代码；
- 普通进程尝试发送 Agent 控制消息；
- 跨组 Agent 读取私有缓存；
- 非租约 owner 提交文件；
- 用户态修改历史记录；
- 伪造 patch 与 test 的因果关系。

plain 目标主要依靠用户态约定和日志；AAA 应由 capability、内核摘要、租约、request/span/cause 和 audit 记录强制检查。

### 结果数据与自动比较

每个目标输出相同的结果文件：

```text
dual-results/
  plain/
    run.json
    metrics.csv
    trace.csv
    final.patch
    test-result.txt
  agentos/
    run.json
    metrics.csv
    trace.csv
    final.patch
    test-result.txt
  comparison/
    summary.csv
    report.md
    raw-manifest.txt
```

`run.json` 至少包含：

```json
{
  "task": "fix-delete-task",
  "status": "passed",
  "patch_hash": "...",
  "test_status": "passed",
  "review_status": "approved",
  "stage_count": 5
}
```

自动比较脚本先检查：

```text
输入 hash 相同
最终 patch hash 相同
测试结果相同
阶段和角色完整
AgentOS 不少于 plain 的成功记录
```

然后再输出性能、可靠性和额外内核证据。不能因为 AgentOS 结果更快，就忽略最终补丁或测试不一致。

### 统计与可复现性

每个目标使用相同 QEMU 参数、CPU 数、文件系统规模和随机种子。每组运行多轮，保存原始结果，并报告：

```text
runs
min
median
mean
P95
max
standard deviation
```

建议参数矩阵：

```text
文件数       = 10 / 50 / 100 / 200
并发 Agent   = 1 / 2 / 4 / 8
查询模式     = first / same-agent-reuse / cross-agent-reuse / invalidated
事件类型     = message / filemod / heartbeat
故障注入     = none / slow-receiver / worker-exit / edit-conflict
```

结果中还要记录：

- 内核镜像大小差异；
- `struct proc` 大小差异；
- 每 Agent 内存开销；
- baseline 与 AAA 用户态业务代码行数；
- plain backend 为模拟 Agent 能力增加的用户态代码量。

最后一项很重要：AgentOS 的价值不一定只表现为 ticks 更少，也可能表现为用户态不再为每个 Agent 重复实现消息队列、权限、版本控制和审计。

### 推荐命令与判定输出

```bash
make dual-codelab
```

建议执行顺序：

```text
检查 baseline 不含 AgentOS 符号
构建两套当前源码
生成相同初始文件系统
运行 plain CodeLab
运行 AgentOS CodeLab
提取结果和原始指标
检查最终结果等价
生成统计和对照报告
```

最终输出：

```text
[DUAL] input_equal=PASS
[DUAL] workflow_equal=PASS
[DUAL] patch_equal=PASS
[DUAL] test_equal=PASS
[DUAL] review_equal=PASS
[DUAL] plain_success_preserved=PASS
[DUAL] agentos_extra_evidence=Context,AgentFS,Event,Capability,Lease,Trace
[DUAL] report=dual-results/comparison/report.md
```

### 对照实验的解释边界

答辩时应明确：

- baseline 是“无 AgentOS 服务、但包含共同通用修复”的 xv6 对照目标，不一定是未经修改的上游 xv6；
- 双目标首先证明功能等价和内核机制替代，性能结论由独立 benchmark 和多轮统计支撑；
- Host 脚本只负责运行、提取和汇总，核心功能必须在 QEMU 内真实执行；
- 页面数、状态文件数和日志行数不是性能优势；
- 结构检查只能证明代码关系存在，不能替代运行测试；
- AgentOS 目标生成更多日志不能自动证明更好，必须说明这些证据解决了什么可信性、安全性或恢复问题。

该方案比单独运行 `agenttest` 更能回答赛题的根本问题：

> 同一个多 Agent 代码修复任务，在没有 AgentOS 时需要哪些用户态约定；加入 AAA 的内核原语后，哪些成本被消除、哪些行为获得了内核级保证？

---

## 3.9 安全性与系统完整性保障

### 这里的“安全性”和“完整性”分别指什么

本文将两者区分为：

```text
安全性：
  未授权主体不能读取、修改、发送、监听或控制不属于自己的 Agent 资源；
  恶意参数、队列洪泛、用户态篡改和并发冲突不能破坏内核或其他 Agent。

系统完整性：
  一次操作要么完整成功，要么失败后不留下部分副作用；
  Agent 在 create/fork/exec/exit、服务退出、超时和异常时状态一致；
  Context、文件版本、消息、工具响应和最终审核之间的因果关系可验证；
  新增 AgentOS 功能不能破坏普通 xv6 进程和原有系统调用。
```

这里的“完整性”不只是“赛题六项都做了”，还包括状态完整性、数据完整性、生命周期完整性和验证证据完整性。

### 四个仓库中值得借鉴的安全与完整性设计

#### AgentOS-coresurgen

值得借鉴：

- Tool Call 使用 typed KV，参数数量、类型和长度更加明确，能在进入具体 handler 前拒绝错误请求；
- 请求和响应有统一状态码，减少不同工具各自解释错误的歧义；
- 赛题要求、demo、脚本和输出之间映射清楚，有利于检查功能覆盖是否完整；
- AgentFS 查询明确区分索引路径和扫描路径，便于发现实现是否退化；
- 自动化脚本检查超时、panic 和关键 PASS 标记。

局限：

- 权限模型不如 AgentOS-uCore 系统化；
- 单消息槽、全局 FILE 事件和静态优先级会影响可靠性与隔离；
- 多 Agent 场景部分依赖 sleep 编排，不能构成强并发完整性保证；
- typed KV 只能提高输入结构安全，不能自动解决授权、生命周期和并发冲突。

AAA 应借鉴其“统一解析、统一错误、统一验收”，但保留动态工具、精确 inode 事件和更强调度。

#### AgentOS-high

值得借鉴：

- Context 与高地址保留区域之间设置未映射 guard page，越界访问直接触发异常；
- 固定 Context header/node ABI，并通过编译期布局断言防止内核与用户态结构静默错位；
- Context 读写按页翻译，覆盖未对齐和跨页缓冲区；
- fork 派生 Agent，复用成熟进程创建路径，减少另写一套加载流程造成的完整性风险；
- 普通 `fork/exec/file/pipe` 回归测试与 Agent 测试一起运行；
- 有界邮箱带 overflow 语义，不用覆盖旧消息掩盖丢失；
- 文档明确记录已知限制，例如 exec 后 Context 映射语义，而不是把未覆盖路径描述为已完成。

局限：

- 用户可写 Context 仍然能被自身篡改，guard page 只防越界，不保证内容可信；
- capability 主要体现在服务发现表达，没有 AgentOS-uCore 那种统一的强制权限系统；
- 调度和文件索引完整性弱于 AAA；
- 某些消息能力较简单，无法承担复杂来源验证和资源隔离。

AAA 应直接借鉴 guard page、固定 ABI、跨页安全和“明确声明已知限制”的工程纪律。

#### AgentOS-uCore

这是安全性和完整性设计最系统的仓库，值得选择性吸收：

- role 与 capability 分离，工具、消息、watch、恢复、metadata 写入和编排权限分别控制；
- 可信程序镜像/启动授权，避免普通进程仅凭调用 syscall 就伪装成高权限 Agent；
- 用户 Context mirror 与内核权威记录分离，并通过 hash chain 检测历史篡改；
- 事件队列记录 source/target/correlation/span/cause，支持来源验证和因果追踪；
- 为内核事件预留容量，并限制外部事件、IPC 类事件和单一来源占用，避免消息洪泛阻塞关键事件；
- 文件编辑 lease、owner、base version 和 stale commit 检查，避免并发覆盖；
- workflow scope 和 VFS credential 隔离，防止不同工作流读取彼此的 metadata、缓存和事件；
- timeline、audit、ledger 和 provenance 记录权限拒绝、事件、Context、调度和文件动作；
- Agent、route、request、lease、scope 和文件描述符在 exit/reap 后回收；
- 对结构体大小、Agent 状态池、内核栈、模块依赖、测试时长和资源配额设置预算门；
- 安全测试不仅检查错误码，还检查文件、队列、权限和状态在拒绝后没有变化；
- 完整 suite 强制运行，不能过滤成单 case 后仍声称整体通过。

不应整体照搬：

- 完整 shadow、sidecar、scope、ledger 和 provenance 会显著增加 PCB、内存和锁复杂度；
- 大型 metadata control plane 超出 AAA CodeLab 的实际需要；
- 过多安全层如果没有清晰 threat model，反而让评委难以判断核心保证；
- 静态结构扫描和文档字段存在性不能替代 QEMU 运行验证。

AAA 应吸收其关键抽象，而不是复制规模：

```text
轻量 capability
可信 Context 摘要
有界且抗洪泛的事件队列
文件 lease/version
request/span/cause
统一 audit
退出自动回收
资源预算
```

#### AgentOS-processnd

值得借鉴：

- Tool Request/Response 和 Context Node 使用固定大小，并有编译期尺寸检查；
- syscall 对跨页用户缓冲区分段翻译，减少只检查首地址导致的越界问题；
- quota 限制 Context 区大小；
- LoopState 的 `Done` 不可逆，结束后 `agent_wait` 返回明确错误；
- `agent_wait` 支持 timeout，不必永久阻塞；
- fork 对 AgentInfo 和优先级的继承行为在代码中明确；
- 普通进程与 Agent 测试共存。

局限：

- `signal_event` 缺少调用者权限检查，普通进程可以伪造事件；
- pending event 是 bitmask，同类事件合并后无法验证数量、顺序和来源；
- 用户态 Context 可以直接修改，没有可信完整性；
- FILE_WRITE 未接入真实写路径；
- fork 虽有继承规则，但 exec、退出清理和跨进程共享语义不够完整；
- 固定 ABI 能防结构错位，但不能代替权限、原子性和资源隔离。

AAA 应借鉴固定 ABI、跨页参数验证、明确的状态迁移和错误返回，但不能采用其无授权 bitmask 事件。

### AAA 建议建立的安全架构

#### 1. 统一身份、角色和 capability

PCB 中至少维护：

```c
int agent_type;
int agent_role;
int agent_group;
uint64 agent_capabilities;
uint64 workflow_id;
uint64 identity_generation;
```

每个工具和控制接口在统一表中声明：

```c
struct agent_tool_policy {
  const char *name;
  uint64 required_cap;
  int group_policy;
  int mutates_state;
  int audit_required;
};
```

检查顺序统一为：

```text
调用者是 Agent
  → 身份 generation 有效
  → capability 满足
  → group/workflow 范围满足
  → 目标对象仍存活
  → 参数合法
  → 才允许产生副作用
```

不要在每个工具内部各自发明一套权限判断。

#### 2. 所有用户输入遵守“先完整验证，再产生副作用”

Tool Call、动态工具、Context、watch、message 和 lease 接口统一遵守：

1. 检查指针范围和长度溢出；
2. 检查完整跨页缓冲区可读/可写；
3. 先 copyin 到内核局部缓冲；
4. 检查字符串终止、枚举范围、数量上限和结构版本；
5. 检查权限和目标生命周期；
6. 预留队列/Context/请求槽位；
7. 最后执行文件、队列或 PCB 修改；
8. copyout 失败时按接口契约回滚或保留可查询结果。

重点避免：

```text
先修改文件
  → 最后 copyout 响应
  → copyout 失败
  → 用户看到失败，但文件已经变化
```

对于会修改文件、metadata、队列或租约的接口，应明确 commit point。

#### 3. Agent Context 分成工作记忆和可信摘要

```text
用户态 Context：
  高频读取、策略缓存、允许 Agent 自主管理；

内核可信摘要：
  sequence/request/span/cause/status
  request_hash/result_hash/previous_hash
  只由内核更新。
```

guard page 防止空间越界，hash chain 防止历史静默篡改，generation 防止读取半更新快照。三者解决的问题不同，不能互相替代。

#### 4. IPC 必须同时具备授权、顺序和抗洪泛能力

FIFO 消息至少包含：

```c
int from_pid;
int target_pid;
int type;
uint64 source_generation;
uint64 sequence;
uint64 request_id;
uint64 span_id;
int length;
char payload[AGENT_MESSAGE_MAX];
```

安全规则：

- 发送者身份和 generation 必须有效；
- group/capability 决定能否发送；
- 目标退出或 PID 槽复用后，旧 endpoint 不能指向新进程；
- 队列满返回 `BUSY`，不能覆盖；
- 单一发送者最多占一定槽位；
- 至少为 FILEMOD/CANCEL 等系统事件保留容量；
- 消费后清理完整槽位，避免旧 payload 泄露；
- dropped、rejected 和 unauthorized 分开统计。

#### 5. 文件修改必须具备授权、版本和原子提交边界

文件写入安全链：

```text
capability(PATCH)
  → path/inode 权限
  → edit lease owner
  → expected version
  → 修改
  → commit
  → version++
  → cache invalidation
  → FILEMOD
  → audit
```

任何一步失败都要定义：

- 文件内容是否变化；
- lease 是否保留；
- version 是否增加；
- cache 是否失效；
- FILEMOD 是否产生；
- Context/audit 记录什么。

最佳语义是：只有 commit 成功后才对其他 Agent 宣布新版本。

#### 6. AgentFS 内部状态需要保护

需要防止用户绕过 Tool Router 直接破坏：

- inode Agent 属性；
- `.agentmeta` 或等价内部索引文件；
- shared query cache；
- metadata version；
- digest/index generation。

建议：

- 内部 metadata 文件不允许普通 open/write/unlink；
- 属性更新统一通过受权限控制的工具；
- cache key 包含 query、group/public、metadata generation；
- cache result 返回前再次检查调用者权限；
- inode 删除、截断、复用和属性修改都增加 generation 或使缓存失效；
- 不以路径字符串作为唯一身份，应使用 `dev/inum/generation`。

#### 7. 生命周期必须覆盖 create/fork/exec/exit/reap

为每个状态明确表格：

| 状态 | create | fork | exec | exit | reap |
|---|---|---|---|---|---|
| Agent 身份 | 初始化 | 继承/清除 | 保留/重建 | 失效 | 清零 |
| capability | 按角色授予 | 降权继承 | 重新认证 | 撤销 | 清零 |
| Context | 映射 | 复制/新建 | 重映射/清除 | 释放 | 无残留 |
| mailbox | 新建 | 不继承旧消息 | 清空 | 拒绝新消息 | 槽位回收 |
| watch | 空 | 不继承/重绑定 | 重新验证 | 注销 | 无悬空 watcher |
| 动态工具 | 空 | 不继承 | 按身份处理 | 自动注销 | 请求失败返回 |
| lease | 空 | 不继承 owner | 中止/重认证 | 自动释放 | 无悬空 owner |
| cache 权限 | 初始化 | 重新检查 | 重新检查 | 无引用 | 无 PID 复用泄露 |

关键是防止 PID 复用攻击。引用目标 Agent 时最好使用：

```text
(pid, identity_generation)
```

而不是只保存 PID。

#### 8. 资源限制必须覆盖所有可增长对象

不能只限制 Context。至少统计和限制：

| 资源 | 建议限制 |
|---|---|
| Context 节点 | quota + FIFO + dropped |
| mailbox | 固定容量 + source limit |
| 动态工具注册 | 每 Agent/全局上限 |
| 动态请求 | 固定队列 + timeout |
| watch | 每 Agent/全局上限 |
| edit lease | 每 Agent/全局上限 + expiry |
| shared cache | 固定条目 + generation invalidation |
| trace/audit | 有界环 + dropped |
| heartbeat | 最小/最大 interval |
| 调度 | quota/budget + aging |

资源耗尽必须返回明确状态，不能 panic，也不能覆盖其他 Agent 的状态。

#### 9. 固定锁顺序，禁止持进程锁读取时钟或操作运行队列

AAA 当前存在一条真实的锁顺序风险：

```text
agent_heartbeat_set：
  p->lock → tickslock

clockintr/wakeup：
  tickslock → p->lock
```

多核下可能形成循环等待。`agent_wait` 持有 `p->lock` 时再读取 `tickslock` 也应一并消除。

可借鉴 AgentOS-high 和 coresurgen 的处理方式：

```text
1. 先读取 now，不持有 p->lock；
2. 获取 p->lock，只修改目标状态；
3. 将需要唤醒的进程保存到局部列表；
4. 释放 p->lock 和其他对象锁；
5. 在锁外执行 wakeup 和 ready queue 入队。
```

统一约束：

- 禁止持有 `p->lock` 时获取 `tickslock`；
- 禁止持有 inode/AgentFS 内部锁时遍历进程并逐个唤醒；
- 禁止持有目标 Agent 锁时操作全局 ready queue；
- 同时涉及多个 Agent 时不嵌套获取两个 `p->lock`，先收集稳定引用，再逐个处理；
- 需要跨模块发布事件时采用“锁内 commit，锁外 publish”。

这比实现通用等待图检测器更适合 AAA：先用固定锁序破坏内核锁死锁，再用 timeout/cancel 处理 Agent 级永久等待。

#### 10. audit 记录必须与执行结果一致

安全相关操作至少记录：

```text
tick
actor pid/generation
target pid/inode/generation
capability
request/span/cause
action
decision=allow|deny
status
commit_version
```

但 audit 不能在用户请求尚未真正 commit 时先写“成功”。推荐：

```text
拒绝：立即记录 deny 和原因；
成功：在 commit point 后记录 success；
部分失败：记录 abort/rollback；
队列满：记录 rejected，而不是 dropped success。
```

### AAA 建议建立的完整性闭环

每个会改变状态的 Agent 操作都使用同一套完整性模板：

```text
1. Validate
   结构、版本、指针、长度、枚举

2. Authorize
   身份、capability、group、目标 generation

3. Reserve
   预留 Context、queue、request、lease 或 audit 槽位

4. Execute
   执行内核操作，但尚不向其他 Agent 发布成功

5. Commit
   更新版本、状态和权威摘要

6. Publish
   cache invalidation、FILEMOD、消息或动态工具响应

7. Record
   Context/audit/timeline 写入最终结果

8. Cleanup
   释放临时资源；失败路径撤销 reserve 和未提交状态
```

典型顺序：

```text
patch commit
  → inode version++
  → AgentFS cache invalidation
  → FILEMOD enqueue
  → Context/audit success
```

不能出现：

```text
先发送 FILEMOD
  → patch 后续失败
  → Reviewer 被虚假事件唤醒
```

### 完整性验证和文档要求

在前述独立实验体系之外，还应增加以下全局检查：

1. **Requirements Traceability**

   每个赛题要求映射到：

   ```text
   内核实现
   用户 ABI
   正向 case
   负向 case
   生命周期 case
   指标
   原始运行日志
   ```

2. **普通 xv6 回归**

   AgentOS 改动后继续运行：

   ```text
   fork/exec/wait
   pipe
   file create/read/write/unlink
   user pointer/bad address
   process exit/reap
   memory exhaustion
   ```

3. **完整 suite 不允许过滤**

   开发时允许单测：

   ```text
   make agent-test CASE=agentlooptest
   ```

   完整验收必须拒绝 CASE 过滤，并生成唯一 run ID 和 manifest。

4. **失败注入**

   在以下时刻 kill Agent 或制造错误：

   ```text
   动态工具 recv 后、reply 前
   lease begin 后、commit 前
   message reserve 后、publish 前
   patch 写入中
   Context 更新中
   cache rebuild 中
   ```

   重启或回收后检查无悬空 request、watch、lease、queue slot 和错误版本。

5. **证据 manifest**

   汇总：

   ```text
   commit hash
   build config
   QEMU 参数
   filesystem seed/hash
   test list
   原始日志 hash
   metrics 文件
   PASS/FAIL
   ```

   防止文档引用旧日志或不同版本的结果。

6. **声明与证据分离**

   文档中区分：

   ```text
   Designed：已有设计但未实现
   Implemented：代码存在
   Tested：独立 case 通过
   Integrated：完整工作流通过
   Benchmarked：多轮数据完成
   ```

   不能因为结构检查发现某个符号，就标记为 QEMU 验证通过。

### 最值得 AAA 直接落地的组合

安全性方面：

```text
capability + group
固定 ABI + 跨页完整验证
Context guard page + 内核可信摘要
FIFO 来源认证 + 系统事件保留
inode lease/version
PID generation
退出自动回收
资源上限
统一 audit
```

完整性方面：

```text
Validate → Authorize → Reserve → Execute → Commit → Publish → Record → Cleanup
create/fork/exec/exit/reap 状态表
正向 + 负向 + 生命周期 + 故障注入
普通 xv6 回归
强制完整 suite
双目标功能非退化
run manifest 与原始证据
```

这能让 AAA 的安全主张从“我们做了权限检查”升级为：

> 未授权操作无法产生副作用；已授权操作具有明确 commit point；并发、退出和失败后无悬空状态；最终结果、Context、事件和审核证据保持一致。

---

# 4. 其他值得实现或补充的设计

## 4.1 Tool schema 和 ABI 版本化

### 来源

- AgentOS-coresurgen：typed KV；
- AgentOS-uCore：typed Tool Call、tool ID、request ID；
- AgentOS-processnd：固定请求/响应尺寸；
- AgentOS-high：`tool_list` 清楚描述接口。

### 建议

不建议推倒 AAA 的字符串 KV 和动态工具，因为动态工具是 AAA 的核心优势。

更稳妥的方式是：

- 保留现有 `tool_call`；
- 给请求增加 `version/request_id`；
- 让 `tool_list` 返回 schema；
- 内置工具统一参数解析和错误码；
- 未来新增兼容的 `tool_call_v2` typed KV。

`tool_list` 建议返回：

```text
name=query_file
params=type:string?,owner:string?,tags:string?,keyword:string?
required_cap=QUERY_FILE
result=paths[],cache_hit,query_plan,candidate_count,elapsed_ticks
errors=BAD_PARAM,PERMISSION,NO_SPACE
```

重点统一：

- 未知工具；
- 缺少参数；
- 类型错误；
- 权限不足；
- 邮箱/动态请求队列已满；
- 工具服务退出；
- 请求超时。

> 不仅列出工具名，还列出参数、返回值和错误说明。
>
> 总之就是，需要返回这个工具怎么用，方便agent进行理解和使用

---

## 4.2 Agent/能力发现

### 来源

AgentOS-high 的自组织 Agent 场景强调运行时发现，而不是由 Planner 硬编码所有 PID。

### 建议

新增结构化工具：

```text
query_agent role=patch
query_agent capability=run_test
query_agent group=1
```

返回：

```text
pid
role
capabilities
group
loop_state
registered_tools
```

CodeLab 中可以由 Planner 查询：

```text
谁能 patch？
谁注册了 run_rule_test_dyn？
谁正在监听 todo.c？
```

这比在启动消息中到处传 PID 更符合多 Agent 操作系统。

---

## 4.3 `agent_spawn` 用户态抽象

### 来源

AgentOS-coresurgen 的 `agent_create(path, ...)` 更符合“创建 Agent 进程”的字面含义。

### AAA 当前问题

AAA 的 `agent_create` 实际是把当前进程升级为 Agent。实现合理，但答辩时容易被追问：

> 为什么 create 没有创建新进程？

### 建议

不重构内核创建链路，先增加用户态：

```c
int agent_spawn(const char *path,
                int type,
                int heartbeat,
                int quota);
```

内部执行：

```text
fork → exec → agent_create
```

文档明确：

- `agent_create`：把当前进程初始化为 Agent；
- `agent_spawn`：创建并启动新的 Agent 程序。

---

## 4.4 可解释的 AgentFS 查询计划

### 来源

- AgentOS-coresurgen：返回索引路径、扫描数量和耗时；
- AgentOS-uCore：返回查询计划和选择原因；
- AgentOS-processnd 的伪索引则说明不能只输出一个 `used_index`。

### 建议

AAA 的 `query_file` 统一返回：

```text
query_plan=cache|index(type,module)|scan
plan_reason=cache_hit|indexed_key|no_index_key|forced_scan
cache_hit=0|1
cache_generation=N
candidate_count=N
scanned_count=N
match_count=N
elapsed_ticks=N
metadata_version=N
```

---

## 4.5 软预算、公平调度与可解释的调度原因

### 来源

- AgentOS-uCore：`ready_age`、`vruntime`、budget penalty、deadline/event 加分、最大连续 Agent burst 和调度 reason flags；
- AgentOS-high：FIFO ready queue，结构简单且天然避免同优先级任务被固定顺序长期跳过；
- AgentOS-coresurgen：多 CPU、多轮调度脚本，但严格静态优先级缺少 aging；
- AgentOS-processnd：三桶队列的桶内 FIFO值得参考，但桶间严格优先级会导致低优先级饥饿。

### AAA 当前风险

AAA 当前调度器会直接跳过 `agent_sched_budget <= 0` 的 Agent，且只在 `best == 0` 时补充所有预算。如果此时始终存在一个普通 `RUNNABLE` 进程，`best` 永远不为空，已耗尽预算的 Agent 就可能永远得不到补充。这不是锁死锁，但会让 Planner、Patch 或 Reviewer 表现为永久卡住。

### 建议机制

不要把 budget 当成硬资格条件，而应作为 score 的软惩罚：

```text
score =
  base_priority
  + event_bonus
  + min(ready_age, AGE_MAX)
  + deadline_bonus
  - vruntime_penalty
  - budget_overrun_penalty
```

具体规则：

1. budget 用尽后仍允许参与选择，只降低 score；
2. 每次运行按权重增加 `vruntime`，累计运行多的 Agent 自然降分；
3. 连续按 score 选择 Agent 达到 `MAX_AGENT_BURST`（可先取 8）后，强制运行 FIFO 队首或等待最久的普通进程；
4. budget 按固定 epoch/tick 周期补充，不依赖“当前是否完全无进程可选”；
5. 如果以后同时运行多个工作流，可把 `agent_group` 当作轻量调度域：组间轮转，组内评分；不需要复制 uCore 的完整 resource domain。

输出选择 Agent 的分数构成：

```text
[SCHED] pid=7 role=Reviewer selected=1
reason=FILEMOD base=60 event=20 aging=4 deadline=0
vruntime_penalty=5 budget_penalty=0 burst=3 score=79
```

---

## 4.6 `agent_wait` 无唤醒源保护和 timeout

### 来源

AgentOS-high 在等待前检测是否存在有效唤醒源；AgentOS-uCore 支持 timeout/cancel；AgentOS-processnd 实现了 `agent_wait(timeout_ms)` 和定时器堆。

### 建议

如果 Agent：

- 没有 heartbeat；
- 没有 watch；
- mailbox 为空；
- timeout 为无限；

则 `agent_wait` 返回明确错误，而不是不可诊断地永久阻塞。

建议接口：

```text
agent_wait(timeout_ticks, event*)
```

返回：

```text
MESSAGE
FILEMOD
HEARTBEAT
TIMEOUT
CANCELLED
NO_WAKE_SOURCE
```

timeout 记录必须包含等待代次，避免事件已经提前唤醒后，旧 timer 再次把同一进程加入 ready queue：

```c
struct agent_wait_timer {
  int pid;
  uint64 identity_generation;
  uint64 wait_generation;
  uint64 deadline;
};
```

timer 到期时只有目标仍存活、仍处于 `AGENT_LOOP_WAITING` 且 `wait_generation` 相同时才允许唤醒。事件、取消或退出提前结束等待时，使旧 generation 失效。

在 CodeLab 中，每个阶段都应设置 deadline 并传播明确失败：

```text
Planner 等 ready 超时      → 终止或重建 Worker
Test 等 Tool reply 超时    → SERVICE_TIMEOUT
Reviewer 等 FILEMOD 超时   → MISSING_FILEMOD
Reviewer 等测试结果超时    → MISSING_TEST_RESULT
```

四个仓库都没有实现通用的多 Agent 等待图死锁检测器。AAA 更适合采用固定锁顺序、有限等待、显式取消和失败传播来预防死锁。

---

## 4.7 动态工具生命周期完善

### 来源

其他项目大多是静态工具，因此 AAA 的用户态动态工具是差异化优势。要让该优势更可靠，需要补齐服务生命周期。

### 建议

- 注册时保存 owner PID、版本、flags 和 required capability；
- 同名注册冲突返回明确错误；
- 服务进程退出时自动注销；
- 请求带 `request_id`；
- 动态请求使用有界 FIFO；
- 请求超时后回收；
- 晚到 reply 返回 `STALE_REQUEST`；
- 调用者被 kill 时清理等待项；
- `tool_list` 展示服务状态和 schema。

---

## 4.8 ADR 设计决策文档

### 来源

AgentOS-high 的 ADR 能解释“为什么这样设计”，而不仅是“代码在哪里”。

### 建议增加

```text
ADR-001：为什么 agent_create 采用当前进程升级
ADR-002：为什么 Tool Call 保留字符串 KV 并支持动态工具
ADR-003：为什么 Context 内容在用户态、摘要在内核态
ADR-004：为什么 AgentFS 采用索引 + shared cache + 版本失效
ADR-005：为什么调度采用 event score + quota + aging
ADR-006：为什么消息使用有界 FIFO 且不覆盖旧消息
ADR-007：为什么代码修改需要 inode 版本和编辑租约
```

每篇只需回答：

1. 问题；
2. 备选方案；
3. 最终决策；
4. 代价；
5. 验证方式。

---

# 5. 扩展优化方向

## 5.1 Tool Call batch

一次提交多个工具请求：

```c
int tool_call_batch(struct agent_tool_request *reqs,
                    struct agent_tool_response *resps,
                    int count);
```

适用于批量查询文件或系统状态，可比较：

```text
N 个独立 syscall
vs
1 个 batch syscall
```

优先级低于 FIFO、权限和文件租约。

## 5.2 Context 节点布局优化

AAA 当前变长节点节省空间，但淘汰可能需要搬移。布局选择应参考前述 Context 数据实验，再决定采用：

- 固定槽位；
- 分段环；
- byte ring + descriptor ring。

不要为了 ABI 整齐，在没有性能证据时直接重构成熟实现。

## 5.3 heartbeat deadline heap/时间轮

Agent 数量增加后，每个 tick 扫描全部 Agent 的成本会变高。可以维护：

- 最小 deadline heap；
- 时间轮；
- 分桶 deadline queue。

该优化的收益主要出现在 Agent 数量较大时，应根据 heartbeat 扫描数据决定是否替换当前实现。

## 5.4 第二种协作拓扑

AAA 已有中心化 CodeLab：

```text
Planner → Retriever/Patch/Test/Reviewer
```

可以增加一个很短的去中心化演示：

```text
Agent 发布 capability
另一个 Agent 查询 capability
运行时形成协作关系
```

重点是证明内核机制通用，不需要再做一个大型业务场景。

---

# 6. 不建议照搬的设计

以下改动风险或投入收益不合适：

- 把 AAA 的动态工具改回静态工具表；
- 用纯 typed ABI 完全替换现有字符串协议；
- 把 AgentFS 降级为静态表或全目录扫描；
- 把 inode 精确监听改成全局文件事件；
- 用严格静态优先级或普通 FIFO 替换现有调度；
- 为每个 Agent 复制完整 Context shadow 和大型 sidecar；
- 实现完整 provenance 图查询系统；
- 实现复杂 resource domain、债务和预留资源体系；
- 用固定 sleep 编排多 Agent；
- 用修改 summary 代替真实代码修复；
- 为第二个 demo 再写一套业务专用内核机制。

判断原则：

> 是否能够补齐赛题要求、CodeLab 可靠性、系统正确性或答辩证据，是判断一项设计是否应进入主线的主要标准。

---

# 7. 推荐的决赛演示主线

```text
1. Planner 通过 capability discovery 找到各类 Agent
   ↓
2. Retriever 首次 query_file
   query_plan=index, cache_hit=0
   ↓
3. Reviewer 重复相同查询
   shared cache_hit=1
   ↓
4. Retriever 第二次处理相同目标
   context_hit=1, skipped_tool_calls=2
   ↓
5. Patch-Agent 获取 todo.c 编辑租约
   另一个 Patch-Agent 得到 CONFLICT
   ↓
6. Patch commit 产生新版本
   shared cache 失效，Reviewer 被 FILEMOD 精确唤醒
   ↓
7. Test-Agent 调用运行时注册的动态测试工具
   ↓
8. Reviewer 根据 diff 和测试结果审核
   ↓
9. Planner 按 span_id 汇总完整因果链
```

终端最终输出：

```text
[PASS] task1 agent/context
[PASS] task2 structured/dynamic tools
[PASS] task3 context path/reuse/quota
[PASS] task4 inode index/shared cache/invalidation
[PASS] task5 mailbox/filemod/scheduler/fairness
[PASS] task6 codelab patch-test-review
[SUMMARY] all requirements passed
```

这条主线既覆盖 proj61 六项任务，也展示 AAA 相对四个对手真正有差异化的能力：

- 动态工具；
- 真实 AgentFS；
- 跨 Agent shared cache；
- Context 复用；
- 事件感知调度；
- 多 Agent 并发安全；
- 真实代码修复；
- LLM/规则策略与内核机制分层。

---

# 8. 最终建议

AgentOS-AAA 当前并不缺少“功能数量”，最需要的是将已有优势进一步变成：

```text
可靠：消息不覆盖、服务退出可恢复、并发修改不冲突
安全：不同角色只有最小 capability
可信：Context 可高速读取，同时有内核可信摘要
可解释：查询计划、调度原因和因果链可以观察
可量化：Context/cache 复用真正减少工具调用和 tick
可验收：一个脚本、一套固定输出覆盖全部赛题要求
```

四个项目的最佳组合不是照搬任何一个仓库，而是：

```text
AgentOS-AAA 的动态工具、真实 AgentFS、shared cache、
事件感知调度、CodeLab 和 LLM Bridge

+ AgentOS-coresurgen 的 typed schema、Context 复用叙事和自动验收
+ AgentOS-high 的 guard page、有界邮箱、能力发现和 ADR
+ AgentOS-uCore 的 capability、文件租约、trace ID 和可信摘要
+ AgentOS-processnd 的简洁状态机、直接 Context 读取和 PASS 输出
```

最终目标不是让 AAA 变成最复杂的项目，而是让它成为：

> 功能闭环最真实、机制边界最清楚、运行最可靠、现场最容易验证的 AgentOS。
