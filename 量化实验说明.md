# AgentOS-AAA 量化实验说明

本文档说明当前仓库中量化实验已经实现了哪些内容、每个实验输出哪些数据、这些数据用于对比什么结论。量化实验不替代功能测试；功能测试回答“功能是否正确”，量化实验回答“机制带来了多少可观察收益”。

## 1. 实验入口

当前量化实验分为 AgentOS 单目标实验和 plain xv6 / AgentOS 双目标对照实验。

AgentOS 单目标实验：

```text
agentfsmetric     AgentFS scan/index/cache 对比
contextmetric     shared query cache 复用与失效
waitmetric        agent_wait 与用户态轮询对比
schedmetric       调度响应性与公平性
mailbench         FIFO mailbox 并发压力
```

双目标 CodeLab 对照实验：

```text
baseline-xv6/user/plainlab.c   原生 xv6 路径
user/planner_agent.c           AgentOS 多 Agent 路径
```

宿主机脚本：

```text
tools/perf/run_agentos_perf.sh     运行 AgentOS 量化实验
tools/perf/run_dual_codelab.sh     运行 plain xv6 与 AgentOS 对照实验
tools/perf/metrics_to_csv.sh       从 raw log 提取 [METRIC] 并转为 CSV
tools/perf/verify_dual_results.sh  校验两条 CodeLab 路径的业务结果等价
```

## 2. 统一输出

所有量化程序都输出机器可解析的 `[METRIC]` 行，例如：

```text
[METRIC] suite=agentfs mode=index files=10 run=1 ticks=0 scanned=2 matches=1 cache_hit=0 status=PASS
```

宿主机脚本会把这些行转换为宽表 CSV，便于导入 WPS / Excel / LibreOffice 画柱状图、折线图和堆叠柱状图。

双目标实验另外输出 `[RESULT]`，用于证明业务结果等价：

```text
[RESULT] suite=dual target=plain found=1 patch_ok=1 test_ok=1 review_ok=1 initial_size=655 initial_hash=8dc6b9e1 file_size=640 file_hash=c0a6e6a2 status=PASS
[RESULT] suite=dual target=agentos found=1 patch_ok=1 test_ok=1 review_ok=1 initial_size=655 initial_hash=8dc6b9e1 file_size=640 file_hash=c0a6e6a2 status=PASS
```

`[RESULT]` 证明两条路径从相同输入得到相同结果；`[METRIC]` 记录性能数据，只有业务结果等价后才允许比较。

## 3. 实验一：AgentFS scan/index/cache

实现文件：

```text
user/agentfsmetric.c
```

实验做了什么：

- 创建一组带 AgentFS 属性的文件；
- 对同一查询分别走 `scan`、`index`、`cache` 三条路径；
- 使用 `nonce` 作为查询元参数区分冷缓存和热缓存，不改变查询语义；
- 输出每条路径的扫描量、匹配数、是否命中缓存。

运行规模：

```text
agentfsmetric        10 个文件，每条路径 2 轮
agentfsmetric small  10 / 30 个文件，每条路径 2 轮
agentfsmetric full   10 / 30 / 60 / 100 个文件，每条路径 3 轮
```

输出数据：

```text
suite=agentfs
mode=scan|index|cache
files
run
ticks
scanned
plan_scanned
full_scanned
matches
cache_hit
used_index
status
```

对比关系：

- `scan`：全量扫描，`scanned` 接近 `full_scanned`；
- `index`：使用属性索引，`scanned` 明显小于 `full_scanned`；
- `cache`：命中 shared query cache，`scanned=0`。

展示结论：

```text
AgentFS 索引减少候选文件扫描；shared query cache 让重复查询不再重新扫描文件系统。
```

当前完整实验已在 QEMU 中跑通：

```text
[SUMMARY] suite=agentfsmetric pass=36 fail=0
```

此前该实验在 10 文件阶段后停止，并非 AgentFS 死锁，而是长时间运行使 Agent 调度分数降为负数，旧 scheduler 因候选初值为 `-1` 而不再选择该进程。修正为“即使最佳候选为负分也可调度”后，软预算只降低优先级，不再形成隐式硬禁用，完整实验可以继续运行到 100 文件阶段。

100 文件阶段的典型结果：

```text
[METRIC] suite=agentfs mode=scan files=100 run=3 scanned=160 full_scanned=160 status=PASS
[METRIC] suite=agentfs mode=index files=100 run=3 scanned=2 full_scanned=160 status=PASS
[METRIC] suite=agentfs mode=cache files=100 run=3 scanned=0 full_scanned=160 cache_hit=1 status=PASS
```

`files=100` 表示实验创建的文件规模。中型 CodeLab 扩容后，内核 `AGENT_FILE_META_MAX` 已从 96 调整为 160，使约 61 个仓库文件、系统程序和测试临时文件能够同时进入索引；该容量仍是固定有界表，不面向 500/1000 文件规模。

两端 mkfs 的磁盘 inode 数同步从 200 调整为 300，确保中型 CodeLab 文件与 `agentfsmetric full` 创建的 100 个实验文件可以共存；这只扩大实验镜像容量，不向 baseline 引入任何 AgentOS 机制。

## 4. 实验二：Context 与 shared cache 复用

实现文件：

```text
user/contextmetric.c
```

实验做了什么：

- 第一次 `query_file` 查询目标文件，填充 shared query cache；
- fork 第二个 Agent，重复执行相同查询；
- 修改文件属性，使旧缓存失效；
- 再执行旧查询，验证不再命中过期缓存。

输出数据：

```text
suite=context
phase=first|cache_hit|invalidated
cache_hit
context_hit
tool_calls
query_file_calls
scanned
plan_scanned
ticks
saved_ticks
matches
status
```

对比关系：

- `first`：首次查询，`cache_hit=0`，需要扫描索引候选；
- `cache_hit`：第二个 Agent 重复查询，`cache_hit=1`，`scanned=0`；
- `invalidated`：属性修改后旧查询不再命中缓存，避免返回过期结果。

展示结论：

```text
跨 Agent 重复查询可以复用 shared query cache；文件属性变化后缓存会失效，保证查询结果新鲜。
```

说明：

```text
当前实验只统计 shared query cache，不夸大 Context 本身的跳过工具调用收益，因此 context_hit 固定输出为 0。
```

## 5. 实验三：plain xv6 与 AgentOS CodeLab 对照

实现文件：

```text
baseline-xv6/user/plainlab.c
user/planner_agent.c
```

实验做了什么：

两条路径执行同一件事：

```text
定位 repo/todo.c
发现 delete_task 缺少 task_count--
修复 todo.c
读取 repo/test.c
执行规则测试
输出最终结果
```

当前镜像会在两端 mkfs 中生成 56 个内容相近的干扰代码文件，加上原有 5 个业务文件，形成 61 文件的中型 `/repo`。干扰文件分属 `auth/store/net/parser/sched/util/cache` 七个模块，并包含 `delete/remove/task/count` 等相近词，避免仅靠关键词或文件名直接定位目标。

plain xv6 路径使用：

```text
目录遍历
open/read/write/stat
sleep/polling
用户态字符串 patch
用户态规则测试
```

AgentOS 路径使用：

```text
AgentFS query_file
多 Agent 消息协作
agent_wait 事件等待
patch_file / read_file / diff_file 工具
动态工具 run_rule_test_dyn
shared query cache
Context Path 记录过程
调度权重配置
```

输出数据：

```text
suite=dual
target=plain|agentos
total_ticks
files_scanned
tool_calls
syscalls
polling_loops
duplicate_queries
cache_hits
cache_misses
query_file_calls
wait_calls
wait_ticks
messages_received
files_read
bytes_read
found
patch_ok
test_ok
review_ok
status
```

对比关系：

- plain xv6：用目录扫描和轮询完成任务；
- AgentOS：用 AgentFS、事件等待、工具调用和多 Agent 协作完成任务；
- 两者都必须输出 `patch_ok=1`、`test_ok=1`、`status=PASS`，先证明业务结果等价，再比较系统机制数据。

### 5.1 等价性校验

plain 与 AgentOS 两端使用内容一致的 `dual_result.h`，对完整 `repo/todo.c` 计算 32 位 FNV-1a 摘要，并验证：

- `delete_task` 仍然存在；
- `task_count--;` 位于该函数内且只出现一次；
- 原缺陷标记已消失；
- 规则测试和最终审核均通过；
- 初始文件大小、摘要相同；
- 最终文件大小、摘要相同。

`run_dual_codelab.sh` 调用 `verify_dual_results.sh`。验证失败时脚本返回非零，不生成用于性能对比的 CSV。一次实际验证结果为：

```text
[VERIFY] suite=dual initial_plain=8dc6b9e1 initial_agentos=8dc6b9e1 initial_equal=1
[VERIFY] suite=dual final_plain=c0a6e6a2 final_agentos=c0a6e6a2 size_plain=640 size_agentos=640 final_equal=1
[VERIFY] suite=dual patch_equal=1 test_equal=1 review_equal=1 business_equivalent=1 status=PASS
```

### 5.2 AgentOS 指标来源

AgentOS CodeLab 的最终指标不是 Planner 中的固定估算值。内核以 `workflow_id` 为单位，在 `kernel/workflow.c` 汇总全部 Worker 的真实数据：

```text
tool_calls
syscalls
messages_received
query_file_calls
files_scanned
index_scanned
cache_hits / cache_misses
duplicate_queries
wait_calls / wait_ticks
```

Tool Router、AgentFS、Agent Loop 和 syscall 分发路径分别插桩。Planner 最后通过只读工具 `get_workflow_metrics` 获取本工作流汇总值；Retriever 和 Reviewer 的阶段日志同时携带真实 `scanned/cache_hit`，可与最终汇总交叉核对。

中型仓库一次实际运行的数据为：

```text
plain:   files_scanned=122 files_read=125 bytes_read=22319 query_count=2
AgentOS: files_scanned=4   files_read=1   bytes_read=511   query_count=3
AgentOS: cache_hits=1 cache_misses=2 duplicate_queries=1
```

baseline 为定位与复核分别完整遍历一次仓库；AgentOS 使用 `tag=delete,module=todo,type=code` 的高选择性属性索引，Retriever 首次查询扫描候选，Reviewer 重复查询命中 shared cache，FILEMOD 后第三次查询重新检查索引候选。两端最终文件摘要和四项业务状态保持一致。

展示结论：

```text
两条路径得到相同修复结果；AgentOS 额外提供语义查询、事件驱动、多 Agent 分工、动态工具和可解释 Context 证据。
```

## 6. 实验四：agent_wait 与轮询

实现文件：

```text
user/waitmetric.c
```

实验做了什么：

- 对 MESSAGE 和 FILEMOD 两类事件分别测试；
- `mode=agent_wait`：Agent 注册事件后通过 `agent_wait` 休眠等待；
- `mode=poll`：用户态循环检查文件状态，模拟传统轮询；
- 记录事件触发 tick、处理 tick、唤醒延迟和轮询次数。

输出数据：

```text
suite=wait
mode=agent_wait|poll
event=message|filemod
delay
run
trigger_tick
handle_tick
wakeup_latency
polling_iterations
cpu_ticks
status
```

对比关系：

- `agent_wait`：`polling_iterations=0`；
- `poll`：`polling_iterations` 明显大于 0；
- 二者都能处理事件，但 agent_wait 不需要用户态空转。

展示结论：

```text
AgentOS 事件等待避免无意义轮询，同时保持低唤醒延迟。
```

## 7. 实验五：调度响应性与公平性

实现文件：

```text
user/schedmetric.c
```

实验做了什么：

- 创建 2 / 4 / 8 个 Agent Worker；
- 设置不同 `priority/quota`；
- 在固定 runtime 内统计各 Agent 的运行推进次数；
- 计算每个 Agent 的等待 tick 分位数；
- 汇总 Jain 公平性指数。

输出数据：

```text
suite=scheduler
agents
pid
priority
quota
dispatch
p50_wait
p95_wait
max_wait
fairness
high_dispatch
low_dispatch
status
```

对比关系：

- 高优先级 Agent 的 `dispatch` 应高于或不低于低优先级 Agent；
- 低优先级 Agent 的 `dispatch > 0`，说明没有长期饥饿；
- `fairness` 使用整数放大 1000，例如 `998` 表示 `0.998`。

展示结论：

```text
高优先级 Agent 更快获得调度机会；低优先级 Agent 仍持续运行，软预算机制避免永久饥饿。
```

## 8. 实验六：FIFO mailbox 并发压力

实现文件：

```text
user/agentmailbench.c
```

执行名：

```text
mailbench
```

实验做了什么：

- 主 Agent 作为接收者；
- fork 1 / 2 / 4 个 sender 并发发送消息；
- 每条消息携带 `sid`、`seq`、`pid`；
- fast consumer 立即消费；
- slow consumer 延迟消费，使 mailbox 更容易满；
- 统计 accepted、received、busy，并验证不乱序、不重复、不串包。

输出数据：

```text
suite=mailbox
senders
messages
consumer=fast|slow
capacity
normal_capacity
sent
accepted
received
busy
order_errors
duplicates
wrong_sender
status
```

对比关系：

- fast consumer：通常 `busy=0`，消息被及时接收；
- slow consumer：mailbox 满后返回 `BUSY`，例如 accepted 接近普通消息槽位数，busy 增加；
- `received == accepted`；
- `order_errors=0`、`duplicates=0`、`wrong_sender=0`。

展示结论：

```text
FIFO mailbox 在并发发送下不会覆盖旧消息；队列满时返回 BUSY，并且已有消息保持 FIFO 和完整性。
```

说明：

```text
AGENT_MAILBOX_CAP=8，其中 AGENT_MAILBOX_SYSTEM_SLOTS=2，所以普通消息可用槽位 normal_capacity=6。
```

## 9. 数据提取与图表

推荐运行：

```text
tools/perf/run_agentos_perf.sh quick
tools/perf/run_agentos_perf.sh small
tools/perf/run_agentos_perf.sh full
tools/perf/run_dual_codelab.sh
```

生成结果目录：

```text
dual-results/run_YYYYMMDD_HHMMSS/
  raw/                    原始 QEMU 输出
  csv/                    宽表 CSV
  dual_verification.log   双目标业务等价校验结果
```

单独转换日志：

```text
tools/perf/metrics_to_csv.sh raw.log > metrics.csv
```

推荐图表：

| 实验 | 推荐图 | 对比重点 |
|---|---|---|
| AgentFS | 折线图 / 分组柱状图 | scan、index、cache 的 scanned/ticks |
| Context/cache | 三阶段柱状图 | first、cache_hit、invalidated |
| CodeLab | plain vs AgentOS 分组柱状图 | files_scanned、files_read、bytes_read、polling_loops |
| agent_wait | 柱状图 / CDF | polling_iterations、wakeup_latency |
| scheduler | 柱状图 / 表格 | dispatch、p95_wait、fairness |
| mailbox | 堆叠柱状图 / 表格 | received、busy、order_errors |

当前不使用 Python 画图。CSV 可直接导入 WPS、Excel、LibreOffice，或后续再接 gnuplot/R 脚本。

## 10. 实验运行规范

正式采数遵守以下规则：

1. 单目标正式实验先重建干净 `fs.img`，再运行 `tools/perf/run_agentos_perf.sh quick|small|full`。
2. 双目标实验运行 `tools/perf/run_dual_codelab.sh`；脚本会强制重建两端的内核和 `fs.img`。
3. 不在已被其他测试写入的 QEMU 镜像中连续采集正式数据，避免 AgentFS 固定容量缓存受到前序测试文件影响。
4. 每次实验保留 raw log、CSV、`run_id`、commit ID、QEMU CPU 数和运行参数。
5. 正式图表至少重复 30 次，报告 median 和 P95；单次 PASS 只作为功能冒烟验证。
6. `ticks=0` 表示操作耗时低于 xv6 tick 分辨率，不代表没有开销。小操作应批量执行或跨多轮统计。
7. plain 与 AgentOS 只有在 `business_equivalent=1 status=PASS` 后才比较性能。

建议运行：

```bash
make -B kernel/kernel fs.img
tools/perf/run_agentos_perf.sh full
tools/perf/run_dual_codelab.sh
```

如果机器运行较慢，可调整等待时间：

```bash
PERF_RUN_SECONDS=2400 PERF_TIMEOUT=2520 tools/perf/run_agentos_perf.sh full
PERF_RUN_SECONDS=300 PERF_TIMEOUT=420 tools/perf/run_dual_codelab.sh
```

## 11. 总结

当前量化实验覆盖了 AgentOS-AAA 的主要性能与系统机制收益：

- AgentFS 索引和 shared cache 减少重复扫描；
- shared query cache 支持跨 Agent 复用，并在文件属性变化后失效；
- CodeLab 对照证明 plain xv6 和 AgentOS 业务结果等价；
- `agent_wait` 避免用户态轮询空转；
- 调度实验展示优先级响应和低优先级不饥饿；
- mailbox 压力实验展示 FIFO、有界队列和 BUSY 语义。

这些数据可以作为答辩中的量化证据，与功能测试、代码结构说明和综合 demo 形成互补。
