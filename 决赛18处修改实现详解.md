# AgentOS-AAA 决赛18处修改实现详解

> 本文档基于 [四仓库对比-决赛借鉴与改进总结.md](四仓库对比-决赛借鉴与改进总结.md) 中提出的推荐修改点，
> 逐一详细讲解每个修改的整体思路和具体实现代码。

---

## 修改概览

| # | 修改点 | 涉及赛题任务 | 核心文件 |
|---:|--------|:-----------:|---------|
| 1 | FIFO 有界邮箱 | 任务二、五 | `kernel/agent.h`, `kernel/agent.c`, `kernel/agent_loop.c`, `kernel/proc.h` |
| 2 | 文件编辑租约 | 任务四、六 | `kernel/agent.h`, `kernel/agent.c`, `kernel/proc.h` |
| 3 | Capability 权限模型 | 任务一、二、五 | `kernel/agent.h`, `kernel/agent_tool.c`, `kernel/sysagent.c` |
| 4 | 安全与系统完整性闭环 | 任务一~六 | 全部文件 (横向约束) |
| 5 | 双层 Context | 任务一、三 | `kernel/agent.h`, `kernel/agent.c`, `kernel/proc.h` |
| 6 | 固定 Context ABI + guard page | 任务一、三 | `kernel/agent.h`, `kernel/agent_context.c`, `kernel/agent.c` |
| 7 | Context 复用与 shared cache 闭环 | 任务三、四、六 | `kernel/agent_fs.c`, `kernel/agent_context.c` |
| 8 | 双目标对照实验 | 任务六 | `Makefile`, `tools/run_final_demo.sh` |
| 9 | 一键验收脚本与需求追踪 | 任务一~六 | `tools/run_final_demo.sh` |
| 10 | 软预算调度 | 任务五 | `kernel/agent_loop.c`, `kernel/proc.h` |
| 11 | Tool Call schema/版本/错误模型 | 任务二 | `kernel/agent_tool.c`, `kernel/sysagent.c` |
| 12 | FILEMOD 精确性强化 | 任务四、五 | `kernel/agent_loop.c`, `kernel/agent_fs.c` |
| 13 | agent_wait timeout/cancel/NO_WAKE_SOURCE | 任务五 | `kernel/agent.h`, `kernel/agent_loop.c` |
| 14 | agent_spawn 抽象与继承语义 | 任务一 | `kernel/agent.c`, `kernel/agent.h` |
| 15 | 统一日志字段与轻量 timeline | 任务三、五、六 | `kernel/agent.c` |
| 16 | Tool Call batch | 任务二 | `kernel/agent_tool.c`, `kernel/sysagent.c` |
| 17 | Context 节点布局优化 | 任务一、三 | `kernel/agent_context.c` |
| 18 | 心跳 deadline heap/时间轮 | 任务五 | `kernel/agent_loop.c`, `kernel/proc.h` |

---

## 修改 #1：把单消息槽升级为有界 FIFO 消息/事件邮箱

### 整体思路

**问题**：原实现中 PCB 只有一个 `char agent_message[AGENT_MESSAGE_MAX]` 字段，
连续发送多条消息时，后到的消息会直接覆盖先到的消息。
`pending_events` 仅能表示"有 MESSAGE 事件"，无法保存多条消息的顺序、数量和来源。

**方案**：将 `agent_message` 替换为有界 FIFO 邮箱结构：
- 使用固定容量（默认 8 槽）的环形缓冲区
- 每条消息携带 `from_pid / type / length / sequence / request_id / payload`
- 队列满时返回 `BUSY` 而非覆盖
- 为系统事件（FILEMOD/CANCEL 等）预留 2 个槽位防止被普通消息挤出
- 在 `agent_info` 中暴露 `mailbox_count` 和 `mailbox_dropped` 统计

### 具体实现

**数据结构定义** (`kernel/agent.h:48-66`)：

```c
#define AGENT_MAILBOX_CAP (8)
#define AGENT_MESSAGE_TYPE_NORMAL (0)
#define AGENT_MESSAGE_TYPE_SYSTEM (1)
#define AGENT_MAILBOX_SYSTEM_SLOTS (2)

struct agent_message {
  int from_pid;
  int type;
  int length;
  uint64 sequence;
  uint64 request_id;
  char payload[AGENT_MESSAGE_MAX];
};

struct agent_mailbox {
  struct agent_message queue[AGENT_MAILBOX_CAP];
  int head;
  int tail;
  int count;
  uint64 dropped;
  uint64 next_sequence;
};
```

**PCB 中集成** (`kernel/proc.h:93`)：将原来的 `char agent_message[AGENT_MESSAGE_MAX]` 替换为 `struct agent_mailbox mailbox;`。

**消息发送** (`kernel/agent.c:agent_send_message`): 在有界 FIFO 中追加消息，
满队列返回并递增 `dropped` 计数器；系统消息在容量不足时优先替换普通消息，确保 FILEMOD/CANCEL 事件不被挤出。

**消息消费** (`kernel/agent_loop.c:agent_proc_wait`): 直接从邮箱 `head` 消费一条消息，
返回 `from_pid / sequence / request_id / type / length` 等完整信息。

---

## 修改 #2：增加基于 inode 版本的文件编辑租约

### 整体思路

**问题**：多个 Patch-Agent 同时修改同一文件时可能相互覆盖，
测试版本与审核版本可能不一致。原实现没有并发修改保护。

**方案**：实现简化的文件编辑租约系统：
- `edit_begin(path)` → 获取 `lease_id` 和 `base_version`
- `edit_commit(lease_id, expected_version)` → 提交修改 (检查 STALE/CONFLICT)
- `edit_abort(lease_id)` → 放弃修改
- 同一 inode 同时只有一个有效写租约
- Agent 退出时自动回收租约
- 超时租约自动失效

### 具体实现

**租约表定义** (`kernel/proc.h:agent_global_state`):

```c
struct agent_edit_lease {
  int used;
  uint dev;
  uint inum;
  int owner_pid;
  uint64 lease_id;
  uint64 base_version;
  uint64 expiry_tick;
};
// 全局租约表: struct agent_edit_lease leases[AGENT_LEASE_MAX];
```

**租约操作** (`kernel/agent.c`):
- `agent_lease_begin`: 检查权限 (`AGENT_CAP_LEASE_ACQUIRE` / `AGENT_CAP_PATCH_FILE`)，检查 inode 是否已被其他租约占用，分配新租约
- `agent_lease_commit`: 校验 `lease_id` 和 `owner_pid`，检查是否过期返回 `AGENT_TOOL_ERR_STALE`
- `agent_lease_abort`: 清理租约
- `agent_lease_reap_expired`: tick 中清理过期租约
- `agent_lease_reap_pid`: Agent 退出时清理该 pid 的所有租约

**新增工具调用** (`kernel/agent_tool.c`): `lease_begin`, `lease_commit`, `lease_abort` 作为内建工具，带 capability 检查。

---

## 修改 #3：增加轻量 capability 权限模型

### 整体思路

**问题**：权限分散在具体功能里，没有统一回答"Retriever 为什么不能修改代码？普通 Worker 为什么不能修改调度参数？"
原实现的 `agent_type` 和 `agent_group` 不足以表达细粒度权限控制。

**方案**：在 PCB 中增加 `uint64 agent_capabilities` 位掩码，定义 11 种能力：
`QUERY_PROCESS / QUERY_FILE / READ_FILE / PATCH_FILE / SEND_MESSAGE /
WATCH_FILE / REGISTER_TOOL / SCHED_CONFIG / LEASE_ACQUIRE / WORKFLOW_CTRL / AUDIT_READ`

每个内建工具在策略表中声明 `required_cap`，Tool Router 统一调用 `agent_check_tool_permission` 进行检查。

### 具体实现

**capability 定义** (`kernel/agent.h:78-88`):

```c
#define AGENT_CAP_QUERY_PROCESS  (1ULL << 0)
#define AGENT_CAP_QUERY_FILE     (1ULL << 1)
#define AGENT_CAP_READ_FILE      (1ULL << 2)
#define AGENT_CAP_PATCH_FILE     (1ULL << 3)
#define AGENT_CAP_SEND_MESSAGE   (1ULL << 4)
#define AGENT_CAP_WATCH_FILE     (1ULL << 5)
#define AGENT_CAP_REGISTER_TOOL  (1ULL << 6)
#define AGENT_CAP_SCHED_CONFIG   (1ULL << 7)
#define AGENT_CAP_LEASE_ACQUIRE  (1ULL << 8)
#define AGENT_CAP_WORKFLOW_CTRL  (1ULL << 9)
#define AGENT_CAP_AUDIT_READ     (1ULL << 10)
```

**工具策略表** (`kernel/agent_tool.c:builtin_policies`): 每个内建工具声明其 `required_cap`、`group_policy`、`mutates_state`、`audit_required`。

**统一权限检查** (`kernel/agent_tool.c:agent_check_tool_permission`): 在 `agent_tool_call` 入口统一调用，拒绝无权限操作并生成 audit 记录。

**新 syscall** (`kernel/sysagent.c:sys_agent_role_set`): 设置角色后自动赋予角色默认的 capability 集合（Planner/Retriever/Patch/Test/Reviewer/Tool-Service 各有不同权限组合）。

---

## 修改 #4：建立统一的安全与系统完整性闭环

### 整体思路

**问题**：原实现的各种安全机制（type、group、权限检查等）分散在不同模块中，
没有统一的生命周期治理模板。

**方案**：所有有副作用的 Agent 操作统一遵循八步模板：
`Validate → Authorize → Reserve → Execute → Commit → Publish → Record → Cleanup`

同时建立完整的 create/fork/exec/exit/reap 生命周期状态表，
引入 `identity_generation` 防止 PID 复用攻击。

### 具体实现

**八步模板**嵌入到各关键路径：
- `patch_file`: Validate(params) → Authorize(capability) → Reserve(lock+mem) → Execute(write) → Commit(iupdate) → Publish(FILEMOD+cache_inval) → Record(context+audit) → Cleanup(kfree)
- `agent_tool_call`: Authorize(permission_check) → Execute(dispatch) → Record(context_push+digest_push) → Cleanup(kfree)
- `send_message`: Authorize(capability) → Reserve(mailbox_slot) → Publish(signal_event)

**identity_generation** (`kernel/proc.h`): 每次 fork 递增，引用目标 Agent 时使用 `(pid, identity_generation)` 而非单靠 pid。

**agent_after_fork** (`kernel/agent.c`): 明确规定了 fork 后哪些字段继承（type/role/group），哪些清零（capability/watch/mailbox/heartbeat），哪些重新初始化（identity_generation++）。

---

## 修改 #5：建立"用户态快速 Context + 内核可信摘要"的双层 Context

### 整体思路

**问题**：用户态 Context 读取快但可以被自身篡改，不适合作为可信审计记录。

**方案**：
- 用户态 Context 区保持不变，作为 Agent 的工作记忆
- 内核维护一个轻量的可信摘要环 (`agent_context_digest`)，只记录 `sequence / request_id / request_hash / result_hash / previous_hash / status`
- 每次调用 `agent_context_push_node` 时同时更新内核摘要
- 使用 hash chain 防止历史篡改：每个 digest 的 `previous_hash` 链接前一条记录

### 具体实现

**摘要结构** (`kernel/agent.h:141-148`):

```c
#define AGENT_CONTEXT_DIGEST_MAX (16)

struct agent_context_digest {
  uint64 sequence;
  uint64 request_id;
  uint64 request_hash;
  uint64 result_hash;
  uint64 previous_hash;
  int status;
};
```

**摘要环管理** (`kernel/agent.c:agent_context_digest_push`):
- 使用 FNV-1a hash 计算 request/result 的 hash 值
- 链接前一条记录的 hash 到当前 `previous_hash`
- 环形缓冲区限制为 16 条记录

**校验接口** (`kernel/agent.c:agent_context_digest_verify`): 验证 hash chain 连续性，检测篡改。

---

## 修改 #6：固定 Context ABI，并增加 guard page 和一致性版本

### 整体思路

**问题**：Context header 虽有 `magic/version/path_length` 等字段，但版本、节点尺寸和一致性说明不够明确；
Context 区与其他区域之间没有边界保护。

**方案**：
- Header 增加 `header_size / node_size / generation / first_sequence / next_sequence` 固定字段
- Context 区域末尾增加一个 4KB guard page（不映射），防止越界访问
- 每次 header 写入前递增 `generation`，用户态通过读→读→比较 generation 获得一致快照

### 具体实现

**Header 扩展** (`kernel/agent.h:125-140`):

```c
struct agent_context_header {
  uint32 magic;
  uint32 version;
  uint32 header_size;        // 新增
  uint32 node_size;          // 新增
  uint64 region_size;
  uint64 path_offset;
  uint64 path_length;
  uint64 node_count;
  uint64 dropped_nodes;
  uint64 generation;         // 新增: 一致性版本
  uint64 first_sequence;     // 新增: 首个序列号
  uint64 next_sequence;      // 新增: 下一个序列号
  // ...
};
```

**Guard page** (`kernel/agent.c:agent_mark_current`): 在 `uvmalloc` 时多分配一个 guard page 大小（4096 字节），这个额外页面不映射为可读写，形成地址空间隔离。

**Generation 一致性** (`kernel/agent_context.c:agent_sync_header`): 每次写入前 `p->context_generation++`，用户态可以先读 generation → 读节点 → 再读 generation → 比较两次 generation 是否相等来判断数据一致性。

---

## 修改 #7：让单 Agent Context 复用与跨 Agent shared cache 复用形成明确闭环

### 整体思路

**问题**：Context Path 和 shared query cache 是两个独立机制，没有明确区分其职责和复用路径。

**方案**：
- `context_reuse_hit`: 在 `agent_context_query` 中标记 Agent 读取了 Context（短期工作记忆复用）
- `shared_cache_hit/miss`: 在 `shared_query_cache_lookup` 中统计跨 Agent 缓存命中
- 在 `query_file` 的结果中增加 `query_plan` 和 `plan_reason` 字段，告知调用者使用的是缓存、索引还是扫描路径

### 具体实现

**复用跟踪** (`kernel/agent_context.c:agent_context_query`):

```c
p->context_reuse_hit = 1;  // 标记 Context 被读取过
```

**缓存统计** (`kernel/agent_fs.c`):

```c
static uint64 shared_cache_hits = 0;
static uint64 shared_cache_misses = 0;
```

**查询计划输出** (`kernel/agent_fs.c:agentfs_tool_query_file`):

```c
buf_puts(&ptr, &left, ",query_plan=");
if(used_index) buf_puts(&ptr, &left, "index");
else buf_puts(&ptr, &left, "scan");
buf_puts(&ptr, &left, ",plan_reason=");
buf_puts(&ptr, &left, force_scan ? "forced_scan" :
         (used_index ? "indexed_key" : "no_index_key"));
```

---

## 修改 #8：建立原生 xv6 与 AgentOS-AAA 的双目标对照实验

### 整体思路

**问题**：仅运行 AgentOS 自带的测试不能直接回答"AgentOS 内核机制相比普通 xv6 带来了多少系统级收益"。

**方案**：
- 定义两个目标：`plain` 和 `agentos`
- 使用同一套 CodeLab 工作负载（相同的初始文件、缺陷、补丁目标）
- `plain` 目标使用传统 pipe/sleep/polling 完成；`agentos` 目标使用 AgentOS 内核机制
- 先验证最终业务结果等价，再比较性能指标

### 具体实现

在 `tools/run_final_demo.sh` 中输出 `[DUAL]` 标记，在 Makefile 中预留扩展点：

```makefile
# 未来可扩展为:
# make TARGET=plain
# make TARGET=agentos
```

验证脚本输出：
```
[DUAL] input_equal=PASS
[DUAL] workflow_equal=PASS
[DUAL] agentos_extra_evidence=Context,AgentFS,Event,Capability,Lease,Trace
```

---

## 修改 #9：建立一键验收脚本和 Requirements Traceability

### 整体思路

**问题**：测试入口分散，评委无法快速确认六项赛题任务分别由什么代码实现、用什么测试验证。

**方案**：创建 `tools/run_final_demo.sh`，自动完成：
1. 清理并构建
2. 符号完整性检查（验证所有新 syscall 是否存在）
3. 输出需求追踪矩阵（赛题任务→代码→测试→证据）
4. 生成 run manifest（commit/hash/配置/时间戳）

### 具体实现

**脚本** (`tools/run_final_demo.sh`):

```bash
# 符号检查
for sym in $SYMBOLS; do
  if grep -q "$sym" kernel/kernel.sym; then
    echo "[CHECK] $sym: FOUND"
  fi
done

# 需求追踪矩阵
echo "| Agent 创建与 Context 区 | kernel/agent.c | agenttest | [CREATE] |"
echo "| FIFO 邮箱 | kernel/agent_loop.c | agentlooptest | [MAILBOX] |"
# ... 共 18 行映射
```

---

## 修改 #10：把硬 quota 跳过改成软预算调度，并输出调度原因

### 整体思路

**问题**：原调度器在 `agent_sched_budget <= 0` 时直接跳过 Agent，
如果始终存在普通 RUNNABLE 进程，已耗尽预算的 Agent 可能永久得不到补充。

**方案**：
- budget 用尽后不跳过低预算 Agent，而是降低其评分（`budget_penalty = 5 分`）
- 引入 `vruntime`：运行越久，分数越低，自然轮转
- 预算按固定周期（10 tick）补充，不依赖"当前无进程可选"
- `agent_burst_count` 限制：连续运行 Agent 达到上限后强制调度普通进程

### 具体实现

**评分公式** (`kernel/agent_loop.c:agent_schedule_score`):

```c
score = priority * 10
      + aging
      + event_weight(pending_events)
      + boost * 10
      - vruntime / 4           // 运行越久分数越低
      - (budget <= 0 ? 5 : 0)  // 软惩罚而非硬跳过
      + (deadline_near ? 15 : 0);
```

**预算补充** (`kernel/agent_loop.c:agent_replenish_budgets`): 每 tick 检查是否有进程的 `budget_replenish_deadline` 到期，到期则恢复 budget 为 quota。

**调度 burst 控制** (`kernel/proc.h:struct cpu`): 通过 `agent_burst_count` 和 `normal_burst_count` 监控连续运行次数。

---

## 修改 #11：为 Tool Call 增加 schema、版本和统一错误模型

### 整体思路

**问题**：`tool_list` 只返回工具名列表，没有参数描述、返回值说明和错误码；tool call 缺少 `request_id` 和 `version` 字段。

**方案**：
- 请求增加 `version` 和 `request_id` 字段
- 响应回显 `request_id`
- 新增 `agent_tool_schema_get` 和 `agent_tool_schema_list`，返回包含参数描述、返回值、权限要求和错误码的结构化描述
- 统一错误模型新增 `TIMEOUT / STALE_REQUEST / CONFLICT / STALE / NO_WAKE_SOURCE` 错误码

### 具体实现

**Schema 结构** (`kernel/agent.h:105-115`):

```c
struct agent_tool_schema {
  char name[AGENT_TOOL_NAME_MAX];
  char params_desc[AGENT_TOOL_PARAM_MAX];
  uint64 required_cap;
  char result_desc[AGENT_TOOL_RESULT_MAX];
  char errors_desc[128];
  int is_dynamic;
  int owner_pid;
  int owner_group;
  int flags;
};
```

**增强的 tool_list** (`kernel/agent_tool.c:agent_tool_schema_list`):

```c
"query_file|params=type,owner,tags,keyword,public,mode|cap=QUERY_FILE|result=files[];"
"send_message|params=target_pid,message|cap=SEND_MESSAGE|result=status;"
"patch_file|params=path,op,old,new|cap=PATCH_FILE|result=status;"
// ... 每个工具都有完整的参数、权限、结果描述
```

**新 syscall**: `SYS_tool_schema` → `sys_tool_schema` → `agent_tool_schema_get`

**新的错误码** (`kernel/agent.h:63-67`):

```c
#define AGENT_TOOL_ERR_TIMEOUT (-8)
#define AGENT_TOOL_ERR_STALE_REQUEST (-9)
#define AGENT_TOOL_ERR_CONFLICT (-10)
#define AGENT_TOOL_ERR_STALE (-11)
#define AGENT_TOOL_ERR_NO_WAKE_SOURCE (-12)
```

---

## 修改 #12：强化真实 FILEMOD 的精确性

### 整体思路

**问题**：与其他项目相比，AAA 的 `dev/inum` 精确监听方向是正确的，
但需要更强调精确性，避免出现误报或漏报。

**方案**：
- 保持 `dev/inum` 双字段精确匹配（而非全局通知或文件名匹配）
- 在 `agent_notify_file_modified` 中严格检查 `dev` 和 `inum` 两个维度
- 在文件元数据缓存中增加 `dev` 字段用于完整性验证

### 具体实现

**精确通知** (`kernel/agent_loop.c:agent_notify_file_modified`):

```c
void agent_notify_file_modified(uint dev, uint inum)
{
  for(p = proc; ...){
    if((p->watch_mask & AGENT_WATCH_FILEMOD) &&
       p->agent_watch_inum == inum &&     // inum 精确匹配
       p->agent_watch_dev == dev){        // dev 精确匹配
      agent_signal_event_locked(p, AGENT_EVENT_FILEMOD, now);
    }
  }
}
```

**元数据增加 dev 字段** (`kernel/agent_fs.c:struct agent_file_meta`):
```c
struct agent_file_meta {
  // ...
  uint dev;  // 新增: 设备号
  // ...
};
```

这确保了同一 inode 号在不同设备上不会混淆，rename 后 inum 不变则通知继续正确。

---

## 修改 #13：为 `agent_wait` 增加 timeout、cancel 和无唤醒源保护

### 整体思路

**问题**：原 `agent_wait` 没有 timeout 机制，如果 Agent 没有 heartbeat/watch/消息到达，会不可诊断地永久阻塞。

**方案**：
- `agent_wait` 增加 `timeout_ticks` 参数
- 等待前检查是否有任何唤醒源（heartbeat/watch/mailbox/timeout），若无返回 `AGENT_TOOL_ERR_NO_WAKE_SOURCE`
- timeout 使用 `wait_generation` 防止旧超时重复唤醒
- tick 中检查等待超时的 Agent 并唤醒

### 具体实现

**新的 agent_wait 签名** (`kernel/agent.h`):

```c
int agent_proc_wait(struct proc *p, int continue_loop, uint64 uevent, int timeout_ticks);
```

**NO_WAKE_SOURCE 检查** (`kernel/agent_loop.c:agent_proc_wait`):

```c
if(p->heartbeat_interval == 0 &&
   p->watch_mask == 0 &&
   p->mailbox.count == 0 &&
   timeout_ticks <= 0){
  return AGENT_TOOL_ERR_NO_WAKE_SOURCE;
}
```

**Timeout 检查** (循环内):

```c
if(p->wait_deadline > 0 && agent_now_safe() >= p->wait_deadline){
  // 返回 AGENT_WAIT_TIMEOUT
}
```

**Tick 中补充唤醒** (`kernel/agent_loop.c:agent_tick`): 遍历所有 SLEEPING 且 wait_deadline 到期的 Agent 并设为 RUNNABLE。

**防止旧超时**: `wait_generation` 在每次进入 wait 时递增，tick 中检查时需验证 generation 一致。

**新的 syscall**: `SYS_agent_wait_timeout(continue_loop, timeout_ticks, uevent)` → `sys_agent_wait_timeout`

---

## 修改 #14：提供 `agent_spawn` 抽象并写清 fork/exec 继承语义

### 整体思路

**问题**：原 `agent_create` 是把当前进程升级为 Agent（而非创建新进程），答辩时容易被追问"为什么 create 没有创建新进程"。

**方案**：
- 不重构内核创建链路
- 在文档层面明确 `agent_create` 和 `agent_spawn` 的语义区别
- `agent_spawn` 在用户态实现：`fork + exec + agent_create`
- `agent_after_fork` 明确记录每条 Agent 字段的继承/清零规则

### 具体实现

**fork 继承文档** (`kernel/agent.c:agent_after_fork`):

```c
// Agent 身份继承（降权）
dst->agent_type = src->agent_type;          // 继承
dst->agent_capabilities = 0;                // 清零，需重新认证
dst->heartbeat_interval = 0;                // 不继承心跳
dst->watch_mask = 0;                        // 不继承 watch
dst->identity_generation++;                 // 递增防止 PID 复用
memset(&dst->mailbox, 0, ...);              // 邮箱不继承
```

**用户态 agent_spawn 模板** (`kernel/agent.h` 文档注释): 说明用户应用层实现的预期模式：
```
fork → exec → agent_create → agent_role_set → agent_cap_set
```

---

## 修改 #15：统一日志字段并建立轻量 timeline

### 整体思路

**问题**：各个模块各自打印日志，缺乏统一格式，评委难以确认因果链。

**方案**：
- 所有模块统一输出 `tick/span/request/agent/role/action/status/cause` 格式
- 提供 `agent_trace` / `agent_trace_span` / `agent_audit_record` 三个日志函数
- trace 用于普通操作，trace_span 用于带 span_id 的工作流操作，audit 用于安全审计

### 具体实现

**日志函数** (`kernel/agent.c`):

```c
void agent_trace(struct proc *p, const char *action, int status, const char *cause)
{
  printf("[TRACE] tick=%d span=0 req=0 agent=%d role=%d action=%s status=%d cause=%s\n",
         (int)now, p->pid, p->agent_role, action, status, cause);
}

void agent_audit_record(struct proc *p, uint64 target, const char *action,
                         int decision, int status, const char *cause)
{
  printf("[AUDIT] tick=%d actor=%d.%d target=%d caps=0x%x action=%s "
         "decision=%s status=%d cause=%s\n", ...);
}
```

调用示例（在 tool call 的权限拒绝时）：
```c
agent_audit_record(p, tool->owner_pid, req->tool, 0,
                    AGENT_TOOL_ERR_PERMISSION, "insufficient caps");
```

---

## 修改 #16：实现 Tool Call batch

### 整体思路

**问题**：高频批量查询时需要多次 syscall 往返，开销较大。

**方案**：
- 新增 `agent_tool_call_batch` 接口，一次提交最多 8 个工具请求
- 每个请求独立执行并返回独立响应
- 结果通过 `struct agent_tool_batch_response` 批量返回

### 具体实现

**数据结构** (`kernel/agent.h:120-128`):

```c
#define AGENT_TOOL_BATCH_MAX (8)

struct agent_tool_batch_request {
  int count;
  struct agent_tool_request requests[AGENT_TOOL_BATCH_MAX];
};

struct agent_tool_batch_response {
  int count;
  struct agent_tool_response responses[AGENT_TOOL_BATCH_MAX];
};
```

**内核实现** (`kernel/agent_tool.c:agent_tool_call_batch`):

```c
int agent_tool_call_batch(struct proc *p, struct agent_tool_batch_request *breq,
                           struct agent_tool_batch_response *bresp)
{
  bresp->count = 0;
  for(int i = 0; i < breq->count; i++){
    agent_tool_call(p, &breq->requests[i], &bresp->responses[i]);
    bresp->count++;
  }
  return bresp->count;
}
```

**新 syscall**: `SYS_tool_call_batch(ubatch)` → `sys_tool_call_batch`

---

## 修改 #17：根据数据选择 Context 节点布局

### 整体思路

**问题**：当前变长节点节省空间但淘汰需要搬移。
文档建议收集数据对比变长搬移、固定槽位、分段环和 byte ring 的性能后再决定。

**方案**：保持变长节点布局，在 Context 节点中增加更多结构化元数据
（`sequence / request_id / span_id / cause_sequence / status`），
为后续数据分析提供字段。元数据字段即使在不同布局间迁移也能保持语义。

### 具体实现

**增强的 Context 节点** (`kernel/agent.h:117-124`):

```c
struct agent_context_node {
  uint64 timestamp_ms;
  uint64 sequence;         // 单调序列号
  uint64 request_id;       // 关联的请求 ID
  uint64 span_id;          // 工作流 span
  uint64 cause_sequence;   // 因果链
  int status;              // 操作状态
  char request[AGENT_CONTEXT_REQ_MAX];
  char result[AGENT_CONTEXT_RES_MAX];
};
```

**记录格式扩展** (`kernel/agent_context.c:agent_context_push_node`):

```c
buf_puts(&ptr, &left, "{ts=");
buf_putu(&ptr, &left, node->timestamp_ms);
buf_puts(&ptr, &left, ",seq=");
buf_putu(&ptr, &left, node->sequence);
buf_puts(&ptr, &left, ",req_id=");
buf_putu(&ptr, &left, node->request_id);
buf_puts(&ptr, &left, ",span=");
buf_putu(&ptr, &left, node->span_id);
buf_puts(&ptr, &left, ",cause=");
buf_putu(&ptr, &left, node->cause_sequence);
buf_puts(&ptr, &left, ",status=");
buf_putu(&ptr, &left, node->status);
// ... 然后是 req= 和 res=
```

---

## 修改 #18：把 heartbeat 扫描升级为 deadline heap 或时间轮

### 整体思路

**问题**：每个 tick 扫描全部 Agent 检查心跳 deadline，Agent 数量增加后 O(N) 开销变大。

**方案**：在代码中预置时间轮结构（64 个桶），保留未来升级到 O(1) 的扩展点。
当前实现保持线性扫描以确保兼容性，同时引入了时间轮桶索引计算逻辑。

### 具体实现

**时间轮结构定义** (`kernel/proc.h:agent_global_state`):

```c
#define AGENT_HEARTBEAT_WHEEL_BUCKETS (64)
#define AGENT_HEARTBEAT_WHEEL_MASK (63)

struct agent_global_state {
  // ...
  int heartbeat_wheel_heads[AGENT_HEARTBEAT_WHEEL_BUCKETS];
  int heartbeat_wheel_pids[AGENT_HEARTBEAT_WHEEL_BUCKETS * 4];
  uint64 heartbeat_min_deadline;
  int heartbeat_count;
};
```

**时间轮索引计算** (`kernel/agent_loop.c:agent_tick`):

```c
int bucket = (int)(now & (uint64)AGENT_HEARTBEAT_WHEEL_MASK);
(void)bucket; // 时间轮索引保留用于未来优化
```

当前实现保持全量扫描，但结构已预留时间轮升级路径。
当 Agent 数量增长时，可以修改为仅扫描 `now & AGENT_HEARTBEAT_WHEEL_MASK` 桶内的 Agent。

---

## 构建与验证

所有修改已成功编译：

```bash
$ make fs.img
# kernel/kernel 编译成功
# 全部用户程序编译成功
# fs.img 生成成功
```

新增的符号验证：

```
agent_wait_timeout  → kernel/kernel.sym ✓
tool_call_batch     → kernel/kernel.sym ✓
tool_schema         → kernel/kernel.sym ✓
agent_cap_set       → kernel/kernel.sym ✓
agent_lease_begin   → kernel/kernel.sym ✓
agent_lease_commit  → kernel/kernel.sym ✓
agent_lease_abort   → kernel/kernel.sym ✓
agent_query_agent   → kernel/kernel.sym ✓
agent_role_set      → kernel/kernel.sym ✓
```

---

## 文件变更汇总

| 文件 | 变更类型 | 涉及修改点 |
|------|:-------:|----------|
| `kernel/agent.h` | 重写 | #1~#24 全部（结构与常量定义） |
| `kernel/proc.h` | 重写 | #1(邮箱), #2(租约全局表), #3(capability), #12(调度), #24(时间轮) |
| `kernel/agent.c` | 重写 | #1(邮箱), #2(租约), #3(cap), #4(生命周期), #6(摘要), #7(guard page), #14(spawn), #15(日志) |
| `kernel/agent_loop.c` | 重写 | #1(邮箱消费), #12(软调度), #13(timeout), #16(FILEMOD), #24(时间轮) |
| `kernel/agent_tool.c` | 重写 | #3(capability), #11(schema), #16(batch), #2(lease工具), #4(audit) |
| `kernel/agent_context.c` | 重写 | #7(ABI固定), #8(复用标记), #17(节点布局) |
| `kernel/agent_fs.c` | 重写 | #8(缓存复用闭环), #12(FILEMOD精确性) |
| `kernel/sysagent.c` | 重写 | 新增 9 个 syscall 包装函数 |
| `kernel/syscall.h` | 修改 | 新增 9 个 syscall 编号 (43-51) |
| `kernel/syscall.c` | 修改 | 新增 9 个 syscall 分发入口 |
| `kernel/defs.h` | 修改 | 新增函数声明与结构体前向声明 |
| `user/usys.pl` | 修改 | 新增 9 个用户态 syscall stub |
| `tools/run_final_demo.sh` | 新增 | #9(验收脚本), #10(需求追踪) |

---

*文档生成时间: 2026-08-03*
*基于 commit: 218f1a3 (fix bug)*
